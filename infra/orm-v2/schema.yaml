title: "JetUse on OCI — かんたんデプロイ v2"
description: >-
  このテンプレートは、JetUseの実行に必要なネットワーク、Autonomous Database 26ai、
  Generative AI Project、IAM、アプリケーションを新規に一括作成します。
  既存のVCN、データベース、Dynamic Group、Policyを指定する必要はありません。
  標準版はDynamic GroupとPolicyも新規作成するため、実行ユーザーにはテナンシIAMを
  管理できる権限が必要です。
  デプロイ先は大阪またはシカゴから選択できます。選択したリージョンが未購読の場合は、
  OCIのリージョン管理からサブスクライブする案内をPlanに表示します。Plan時に権限、
  リージョン購読、Identity Domainの状態を確認し、合格後にリソース作成へ進みます。
schemaVersion: 1.1.0
version: "20260817"

logoUrl: ""
locale: "en"

variableGroups:
  - title: "デプロイ先"
    variables:
      - deployment_region
      - compartment_ocid
      - admin_email
  - title: "サインイン設定"
    variables:
      - identity_domain_mode
      - existing_identity_domain_ocid
  - title: "Resource Managerが設定する内部値"
    visible: false
    variables:
      - tenancy_ocid
      - region
      - image_tag

variables:
  deployment_region:
    type: enum
    title: "デプロイするリージョン"
    description: >-
      大阪またはシカゴを選択してください。大阪を推奨します。
      選択したリージョンをまだ利用していないテナンシでは、Apply前にOCIコンソールの
      「リージョン管理」からサブスクライブする必要があります。
    enum:
      - 大阪（ap-osaka-1・推奨）
      - シカゴ（us-chicago-1）
    default: 大阪（ap-osaka-1・推奨）
    required: true

  compartment_ocid:
    type: oci:identity:compartment:id
    title: "コンパートメント"
    description: "JetUse専用リソースを新規作成するコンパートメントを選択してください。"
    required: true

  admin_email:
    type: string
    title: "初期管理者のメールアドレス"
    description: "作成するJetUse初期管理ユーザーのメールアドレスです。"
    required: true

  identity_domain_mode:
    type: enum
    title: "Identity Domainの準備方法"
    description: >-
      「新しく作成（推奨）」はJetUse専用の無料Identity Domain、ログインアプリ、
      初期管理ユーザーを作成します。Identity Domainを作成できる管理者権限が必要です。
      「既存のIdentity Domainを使用」は選択したDomain内にJetUse専用ログインアプリと
      初期管理ユーザーを作成し、Domain自体は変更・削除しません。
    enum:
      - 新しく作成（推奨）
      - 既存のIdentity Domainを使用
    default: 新しく作成（推奨）
    required: true

  existing_identity_domain_ocid:
    type: oci:identity:domains:id
    title: "使用するIdentity Domain"
    description: "ACTIVEな既存Identity Domainを選択してください。Domain自体はTerraformの管理対象にしません。"
    required: true
    dependsOn:
      compartmentId: ${tenancy_ocid}
    visible:
      eq:
        - ${identity_domain_mode}
        - 既存のIdentity Domainを使用

  tenancy_ocid:
    type: oci:identity:tenancy:id
    title: "Tenancy"
    visible: false

  region:
    type: oci:identity:region:name
    title: "Region"
    visible: false

  image_tag:
    type: string
    title: "Container image tag"
    default: "latest"
    visible: false

outputs:
  app_url:
    type: link
    title: "JetUse URL"
  demo_username:
    type: string
    title: "初期管理ユーザー"
  demo_password:
    type: string
    title: "初期パスワード"
  identity_domain_url:
    type: link
    title: "Identity Domain URL"
  project_ocid:
    type: string
    title: "Generative AI Project OCID"
  preflight_result:
    type: string
    title: "事前確認結果"

outputGroups:
  - title: "デプロイ結果"
    outputs:
      - app_url
      - demo_username
      - demo_password
      - identity_domain_url
      - project_ocid
      - preflight_result
