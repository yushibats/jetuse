# 改善要望（ER）

**目的**: 実装した方がよいと気づいたことを**その場で置いておき**、後から取りこぼさないための置き場。
いま進めている実装に集中できるように、**ここは急かさない**（既定の状態は「積んである」）。

- **1件1ファイル**（`ER-XXXX-<短い題>.md`）。詳細はリポジトリ側に置く（実装者が読む）
- **概要は Obsidian の `_renders/ER/`** で見る（判断する人が読む）。生成は `ops/er.py report`
  - `index.md` … **Markdown**。Obsidian でそのまま読めて**リンクが効く**
  - `ER-XXXX.html` … 1件ずつの詳細。表や強調を整えた**説明として読む**形
- **この一覧は自動生成**（`ops/er.py index`）。手で編集しない

## 使い方

```bash
python3 ops/er.py list                 # 一覧を見る
python3 ops/er.py index                # この README の表を更新する
python3 ops/er.py report               # Obsidian の _renders/ER/ へ index.md と詳細 HTML を出す
```

**思いついたら書く。** `_TEMPLATE.md` を写して番号を振り、`ops/er.py index` を実行する。
**その場で判断しない**——`status: parked` のまま積んでおき、
次に何をやるか決めるときに Obsidian のレポートから選ぶ。

## 止まっている作業も同じレポートに出る

`ops/er.py report` は、ER に加えて **`jetuse-loops/` の worktree の状態**も出す
（`ops/stalled.py`）。判定は**リポジトリの実際の状態だけ**から作る
（人が更新する台帳は必ずずれるので作らない）。

| 状態 | 意味 |
|---|---|
| **レビューが終わっていない** | **放っておくと欠陥が本番に残る** |
| **PR が出ていない** | レビューは通ったが出荷されていない |
| 意図的に止めている | ER が理由を書いている（把握済みなので急がない） |

**実害**: 2026-08-04 に、レビュー未完了のまま **6 日放置**されていた作業から、
**マージ済みコードの blocker が 2 件**見つかった（他人の資産を消しうるもの）。
「思いつき」だけでなく「やりかけ」も取りこぼさないようにする。

**意図的に止めるときは、ER 本文にブランチ名を `` `feat/xxx` `` の形で書く。**
そう書いてあるものは「放置」ではなく「保留」として扱われる。

## 着手するとき

1. ER を読み、`tasks/<ID>.md` に作業指示書を書く（完了条件・E2E・非ゴールまで）
2. ER の `status` を `doing` に、`ticket` に作業指示書のパスを書く
3. `[GOAL="..."] .claude/loop/start-loop.sh <ID>` でループを起こす
4. PR が出たら ER の `pr` を埋め、マージ後に `status: done`
5. `ops/er.py index && ops/er.py report` で両方を更新する

**`status` と `pr` を書き換えたら必ず両方を再生成する。** 手で表を直すとずれる。

## 書くときの約束

- **顧客名・顧客固有の固有名詞を書かない**（社名・社内システム名・顧客の文書名・社内コード体系）。
  一般語に伏せる。ER は git に載るため。
- **「実害」は実際に踏んだものだけ**。想像で書いたものは `気づき` か `構想` にする。
  この区別が、後から拾うときの優先順位になる。
- 根拠に**数字を書く**。推測なら「推測」と明記する。

<!-- BEGIN INDEX -->
| ID | 状態 | 種別 | 大きさ | 内容 |
|---|---|---|---|---|
| [ER-0001](ER-0001-配備の雛形に必要な設定が無い.md) | 積んである | 実害あり | 小 | 配備の雛形に必要な設定が無い |
| [ER-0002](ER-0002-ブラウザから直接呼べない.md) | 積んである | 実害あり | 小 | ブラウザから直接呼べない |
| [ER-0018](ER-0018-共有ADBが予告なく停止していた.md) | 積んである | 実害あり | 小 | 共有 ADB が予告なく停止していた |
| [ER-0004](ER-0004-認証つきでコーディングエージェントから呼べるようにす.md) | 積んである | 気づき | 中 | 認証つきでコーディングエージェントから呼べるようにする |
| [ER-0005](ER-0005-Word_PowerPointを取り込めるようにする.md) | 積んである | 気づき | 中 | Word / PowerPoint を取り込めるようにする |
| [ER-0006](ER-0006-行レベルのアクセス制御を実証する.md) | 積んである | 構想 | 中 | 行レベルのアクセス制御を実証する |
| [ER-0007](ER-0007-会話の記憶をデータベース側に持たせる.md) | 積んである | 構想 | 大 | 会話の記憶をデータベース側に持たせる |
| [ER-0008](ER-0008-ユースケースを組み立てられる基盤にする.md) | 積んである | 構想 | 大 | ユースケースを組み立てられる基盤にする |
| [ER-0009](ER-0009-承認を挟む使い方で検索の予算を数え違える.md) | 積んである | 気づき | 小 | 承認を挟む使い方で検索の予算を数え違える |
| [ER-0010](ER-0010-モデル比較のハーネスが順序を検査していない.md) | 積んである | 気づき | 小 | モデル比較のハーネスが順序を検査していない |
| [ER-0011](ER-0011-OpenAPIを認証つきで公開する.md) | 積んである | 気づき | 小 | OpenAPI を認証つきで公開する |
| [ER-0013](ER-0013-ページ送りトークンをURLエンコードしていない.md) | 積んである | 気づき | 小 | ページ送りトークンをURLエンコードしていない |
| [ER-0014](ER-0014-配備スクリプトのビルド設定が二重になっている.md) | 積んである | 気づき | 小 | 配備スクリプトのビルド設定が二重になっている |
| [ER-0016](ER-0016-registryコンパートメントに不要な権限が向いている.md) | 積んである | 気づき | 小 | registry コンパートメントに不要な権限が向いている |
| [ER-0017](ER-0017-開発用OCIRが公開設定になっている.md) | 積んである | 気づき | 小 | 開発用 OCIR が公開設定になっている |
| [ER-0019](ER-0019-CIのビルドが毎回依存を入れ直している.md) | 積んである | 気づき | 中 | CI のビルドが毎回依存を入れ直している |
| [ER-0003](ER-0003-配備スクリプトが前提ツールと合っていない.md) | 完了 | 実害あり | 小 | 配備スクリプトが前提ツールと合っていない |
| [ER-0015](ER-0015-環境を作る手順にマイグレーションが無い.md) | 完了 | 実害あり | 小 | 環境を作る手順にマイグレーションが無い |
| [ER-0020](ER-0020-レポート生成器が入力次第で固まる.md) | 完了 | 実害あり | 小 | レポート生成器が入力次第で固まる |
| [ER-0021](ER-0021-未リリース分が一覧に出ない.md) | 完了 | 実害あり | 小 | 未リリース分が一覧に出ない |
| [ER-0012](ER-0012-公開リポジトリに実OCIDが残っている.md) | 完了 | 気づき | 小 | 公開リポジトリに実 OCID が残っている |
<!-- END INDEX -->
