# 発見・Tipsメモ（蓄積用）

実機検証で得た未文書仕様・ハマりどころ・運用Tipsを日付付きで蓄積する。
詳細があるものは検証レポート等へのリンクを付ける。**新しい発見はこのファイルに追記すること**（運用ルール 2026-06-11 ユーザー指示）。

## OCI Enterprise AI（OpenAI互換API）

- **2026-06-10 ⭐ Conversationsの履歴圧縮は未文書フラグで制御**: `metadata.short_term_memory_optimization`（既定 `"false"` が自動付与される）。`"true"` でコンテキスト約2.5kトークン超から圧縮が発動し、**長会話の累計入力トークン42%削減・圧縮後も記憶保持OK**を実測。JetUseでは既定有効化済み（CHAT-06b）→ docs/verification/CP2-measurements.md
- 2026-06-10 Conversationオブジェクトに**retention/TTLの公開フィールドはない**（クライアントから保持期間は制御不可）。能動的に消す手段は削除のみ → 会話削除時の同期削除を実装（CHAT-09）
- 2026-06-10 **記憶保持の計測プローブに個人情報様の値（社員番号等）を使わない**: 圧縮と無関係にgpt-ossが「個人情報は保持できません」と拒否し、記憶喪失と誤判定する。中立的な事実（プロジェクトのコードネーム等）を使う
- 2026-06-10 TTFT実測（API GW経由・中央値）: llama-3.3-70b 0.17s / gemini-flash 1.04s / gpt-oss-120b 1.26s / **gemini-2.5-pro 5.80s**（keepalive 15秒設計の根拠）
- **2026-06-11 gpt-oss-120bはtemperature≧1.9で推論が暴走し本文が一切出ない**（1.9: 出力1,970tokが全て内部推論で本文ゼロ完走 / 2.0: 90秒無応答。1.5は正常）。推論系モデルの実用上限は1.5 → UIスライダー上限を1.5に変更（CHAT-03c）
- **2026-06-11 LLM生成のmermaidは「ノードラベル内の未クオート括弧」で構文エラーになりがち**（実測: gpt-ossが `C[POST (Power‑On Self Test)]` を生成）。正しくは `C["..."]`。クライアント側でparse失敗時に自動クオート→再parseで救済できる（markdown.tsx repairMermaid）。それでも不正なら生成内容の問題
- 2026-06-11 highlight.jsにTerraform/HCL文法は存在しない（commonにも全量にもない）→ カスタム文法を登録する（markdown.tsx hcl()）
- **2026-06-11 サンプリングパラメータの実機対応マトリクス**: Responses系(gpt-oss)= `top_p` / `max_output_tokens` / `reasoning.effort`(low/highで推論トークン量が変わることを確認、43↔81tok) すべてOK。Chat Completions系(gemini/llama)= `top_p` / `max_tokens` / `stop` / `frequency_penalty` / `presence_penalty` / `seed` すべてOK。ペナルティ・stop・seedはResponses系に存在しないためUIはモデル系統で出し分けが必要
- **2026-06-11 Gemini系は小さいmax_tokensで死ぬ**: 思考トークンがバジェットを食うため、max_tokens=100はストリーム無応答（90秒+）、512でも本文空（finish=max_tokens）、2000で正常。`reasoning_effort=minimal` は受理されるが思考は止まらない（none/lowは400）。対策=実用下限2048でサーバー側クランプ（CHAT-04b、models.py min_max_tokens）
- **2026-08-20 Gemini系の切れた応答は「空」ではなく「途中まで」で返る**: `max_tokens=1024` の要約が文の途中（「…次に、コンピュータ」）で終わり、**成功応答として返った**（VID-03 実測）。JSON を要求している経路は parse で落ちて気づけるが、**素の文章は切れても気づけない**。`finish_reason == "length"` を見て失敗扱いにする（`video_analyze._content`）。上限そのものは思考トークンぶんを見込んで 4096（既存の「小さいmax_tokensで死ぬ」の続き）
- **2026-08-20 ffmpeg の drawtext は `text=` に `:` を書くと落ちる**: テロップの `12:34` がフィルタの区切りと解釈され exit 234。`textfile=<パス>` で渡せば日本語も含めて素通し（E2E 素材の生成で踏んだ）
- 2026-06-11 SSEのproduceスレッドで `stream_chat()` 呼び出しをtry外に置くと、同期例外時に終端イベントが送られずkeepaliveで永久ハングする（テストfakeの引数不一致のTypeErrorで発覚）。**ジェネレータ生成もtry内+except時にerrorイベント送出**が正解

## 認証（Identity Domains / oidc-client-ts）

- **2026-06-11 oidc-client-tsのsilent renewは既定で危険**: `maxSilentRenewTimeoutRetries` 未設定だとiframeタイムアウト時に**無制限に5秒間隔でリトライ**する（ソースで確認）。さらに専用silent redirect URIがないとSPA本体がiframeに読み込まれ状態を壊す。**自動リダイレクト系の処理には必ずループ保険（時間ガード）を入れる** → INFRA-02c: iframe全廃+期限前の単発signinRedirect+30秒ガード（specs/06-oidc.md）
- 2026-06-10 IDCSの `prompt=none`（セッションなし）はクリーンな302 `?error=login_required` を返す（実測）。エラーリダイレクトのハンドリング必須
- 2026-06-10 M2Mテストアプリのclient secretは `oci identity-domains apps list --attributes clientSecret` で取得できる（紛失時の再発行不要）。トークンは `POST {domain}/oauth2/v1/token` に client_credentials + `scope=urn:opc:idm:__myscopes__`

## OCI運用

- **2026-06-10 夜間停止はADBを巻き込み、ADBは自動再開しない**（computeと違う）→ 翌朝アプリ全死。`ops/start-adb-if-stopped.sh` を用意（cron登録は人間判断、backlog #10）。**セッション開始時に挙動が変な時はまずADBの状態を疑う**
- 2026-06-10 DB停止時にoracledbはタイムアウト未設定だと**無限ハング**する。`tcp_connect_timeout` / `POOL_GETMODE_TIMEDWAIT+wait_timeout` / `call_timeout` の3層設定が必要（CHAT-07）。プールはADB再開後に自動回復する（CI再起動不要）
- 2026-06-10 ~~`.env`のADB_OCIDがスパイクADBを指す問題~~ **解消**: スパイクADB2本は削除済み（2026-06-11ユーザー指示）、ADB_OCIDはjetuse-dev-adbに修正済み。jetusedevウォレットは非公開バケット `jetuse-dev-app-data` の `adb_wallet.zip`（/home/opc/adb_wallet/は旧スパイク用で無効）
- **2026-06-11 jetusedevウォレットのewallet.pemはパスワード保護**: `wallet_password` 未指定だとoracledb thinが**stdinでPEMパスフレーズを無限プロンプト**し、プール経由だと背景スレッドで沈黙→DPY-4005に見える（実害: migrate実行が謎ハング）。パスワードは tfvars の `ADB_WALLET_PASSWORD`。migrate実行例は docs/verification/UC-01-03.md 参照
- 2026-06-11 プールの `wait_timeout=5000` はコールドmTLS接続（>5秒）で不足しDPY-4005になる → 15秒に変更。DB停止の即時503は `tcp_connect_timeout` 側が担うので影響なし

## 開発環境

- **2026-06-10 夜間停止が `npm install` 中に当たるとnode_modulesに0バイトファイルが残骸として残る**。`npm install` のやり直しでは直らない（npmはパッケージ存在扱い）→ `rm -rf node_modules && npm cache verify && npm ci` で復旧
- 2026-06-10 FastAPIのSSEで**クライアント切断のテストはTestClientでは不可能**（全身を読み切ろうとしてハング）。実uvicornをスレッドで立てて httpx で途中切断する（packages/api/tests/test_cancel.py）
- 2026-06-10 API GWは**クライアント切断をバックエンドに伝搬する**（実測）。サーバー側で切断検知→上流LLMストリーム打ち切りが機能する（CHAT-08）

## フロントエンド

- 2026-06-11 **配列を「カンマ区切りの制御input」で編集させると、onChangeで即split/joinする実装はカンマ入力自体が消えて1要素しか作れない**（ビルダーの選択肢で実発生）。区切り文字方式は生文字列のままstateに持つか、最初から「＋追加」方式の個別inputにする
- **2026-06-11 OCIのストリーミングは一時エラーを非JSON（単引用符dict様）ペイロードで流すことがあり、openai SDKの解析がJSONDecodeError（"Expecting property name enclosed in double quotes"）で落ちる**（RAGチャットで間欠的に実発生、12連続では再現せず）。対策: stream_chatでJSONDecodeErrorを個別捕捉し、未出力なら1回自動リトライ・途中なら平易なメッセージ表示・tracebackをログに残す（task/rag-fix-1）
- **2026-06-11 Select AIのベクトル索引（RAG）はADB 23ai必須**: 19cでは `ORA-20047: Oracle vector index is not support in current database version`。CREATE_PROFILEまでは19cでも通ってしまい、CREATE_VECTOR_INDEXで初めて発覚する
- 2026-06-11 Select AI RAGの実機セットアップ: profile（`embedding_model` + `vector_index_name`）→ `CREATE_VECTOR_INDEX`（`vector_db_provider:'oracle'`、バケットURL直指定、credentialはSPIKE-04のAPIキー方式）。索引進捗は `{INDEX}$VECTAB` の行数。`DBMS_CLOUD_AI.GENERATE` の戻りはCLOB（fetch_lobs=False必須）。応答にSources（ファイル名+URL）が自動付与。実測: 3文書20秒で索引、10/10正答、中央値1.6s（spikes/spike08_select_ai_rag.py）
- **2026-06-11 ADB 19c→新版アップグレードの実際**: terraformやCLIで `db_version` を同期更新すると `ADB-00105: Sync-upgrade not allowed, please use schedule-upgrade API`。正解は `oci db autonomous-database update --db-version 26ai --is-schedule-db-version-upgrade-to-earliest true`（**ターゲット版の明示必須**、アップグレード先は23aiではなく26ai=2026年時点の最新長期版）。「earliest」で約30分後にスケジュールされた（実測）。新規作成なら23aiも指定可
- **2026-06-11 Select AIベクトル索引を非ADMINユーザーで使うには `DBMS_CLOUD_PIPELINE` のEXECUTEも必要**（索引同期がパイプライン実装。`ORA-20000: Missing EXECUTE privilege on DBMS_CLOUD_PIPELINE` で発覚）。DBMS_CLOUD / DBMS_CLOUD_AI / DBMS_CLOUD_PIPELINE の3点セット+ GenAI/OSホストのACL（ops/setup-select-ai.py）
- 2026-06-11 DBMS_CLOUD_AIのCREATE_PROFILEが成功しCREATE_VECTOR_INDEXが失敗すると**プロファイルだけ残り、遅延作成ロジックが索引なしで素通りする** → 作成は冪等チェックをプロファイル基準でなく索引基準にするか、失敗時にDROP_PROFILEすること
- **2026-06-11 SQL Search enrichのボディはAPI文書なし、SDKモデルから確定**: `{displayName, enrichmentJobType:"FULL_BUILD", enrichmentJobConfiguration:{enrichmentJobType:"FULL_BUILD", schemaName:"SH"}}`。誤フィールドは"Content parsing error!"だけ返り手掛かりなし → `oci.generative_ai_data.models.GenerateEnrichmentJobDetails` を見るのが正解
- 2026-06-11 **mTLS必須ADBへのDBTools接続**は cwallet.sso のbase64をVault secretにし `--key-stores '[{"keyStoreType":"SSO","keyStoreContent":{"valueType":"SECRETID","secretId":...}}]'` で可能（validate OK実証）。パスワードは `--user-password-secret-id`。semantic-storeの一覧は `semantic-store-collection list-semantic-stores`
- **2026-06-11 Select AI (NL2SQL)は26ai+llamaで大幅改善**（10/10生成・中央値1.8s）が、**四半期の取り違え・フィルタ誤りは残存**（10問中2問誤答。SQL Searchは10/10）。正確性はSQL Search、速度はSelect AI。比較: docs/comparison/nl2sql-backends.md
- 2026-06-11 API GW経由の30秒級SSEをバッチ連続実行すると間欠的に `peer closed connection without sending complete message body` が出ることがある（単発では再現せず）。クライアント側リトライで運用可（backlog #12）
- **2026-06-11 Responses APIのツール実機対応**: カスタムfunctionツール完全動作（function_call_outputは元のfunction_callアイテムと共にinputへ）。ストリーミングは `response.output_item.done` でfunction_callの完成形が取れる（デルタ結合不要）。**code_interpreter built-inは動作**（`{"type":"code_interpreter","container":{"type":"auto"}}`、OCI側サンドボックス）。**web_search built-inは不可**（"only supported for OpenAI provider models"）→ 検索はカスタムツールで自前実装（SPIKE-09）
- 2026-06-11 **Web検索ツールはDuckDuckGo HTML版（html.duckduckgo.com/html/ にPOST）でAPIキー不要で実装可能**（result__a/result__snippetクラスをパース、uddg=リダイレクト展開が必要）。OCIのweb_search built-inは使えないため（SPIKE-09）この方式を採用（AGT-01）
- 2026-06-11 gpt-ossはツールがエラーJSONを返した直後の最終回答が**空になることがある**（agent_doneまで0.5秒で完了する形）。UIの空応答注記+再生成で運用。function_call_output提出時はfunction_callアイテム（type/name/arguments/call_id）を必ずペアで入れる
- **2026-06-11 OpenAI互換APIのフィールド探索の罠**: OpenAI本家に存在するフィールド（`user`等）は「not supported」と明示拒否されるが、**完全に未知のフィールドはextra_bodyで黙って無視される**（`memory={}`等のデタラメでも受理）。「受理された」を機能の証拠にしないこと — デタラメフィールドの対照実験が必須（SPIKE-10で subject_id 長期メモリの不在を確定する際の教訓）
- **2026-06-11 長期メモリの正しい使い方（SPIKE-10bで確定、初回探索は空振り）**: ①プロジェクト作成時に `--long-term-memory-config` 必須（**後から変更不可**と明示エラー。STM condenser/retentionも作成時のみ）②モデルは名前形式かつ制約あり（extraction=gpt-oss可・llama不可、embedding=cohere.embed-v4.0のみ可）③利用は**会話metadataに `memory_subject_id`**（responsesパラメータではない）。約10-15秒で抽出され別会話から想起可能。プロジェクト再作成時はFiles/Vector Storeの再取り込みとoci_conversation_idのNULL化が必要（移行手順はAGT-05.md）
- 2026-06-11 会話metadataの `short_term_memory_optimization` 既定値は**プロジェクトのSTM condenser設定で変わる**（設定なし=false / condenser設定あり=true。jetuse-dev-projectで実測）。アプリは明示trueで固定しているため挙動は安定
- **2026-06-11 長期記憶は源泉会話に紐づく（未文書）**: 会話を削除すると、その会話から抽出された長期記憶も消える（実測。削除後20秒で忘却）。ただし**別の会話で事実が復唱されているとそちらが源泉として残る**（アシスタントの想起回答も源泉になる）。完全忘却には言及会話を全て削除する必要あり
- **2026-06-11 Responses APIは type:"mcp" ツールでリモートMCPをサーバーサイド実行できる**（SPIKE-11）。`require_approval:"always"`→`mcp_approval_request`、継続inputは「userメッセージ+承認アイテム+mcp_approval_response」（**userメッセージ必須**）。`never`なら1レスポンス内でlist_tools→複数mcp_callまで完結
- 2026-06-11 GW経由の長時間SSE間欠切断は**keepaliveをdataフレーム化しても根絶しない**（5連続で1件残存）→ 経路側事象と結論。実用対策は「本文未受信ならクライアント自動リトライ」（読み取り専用ツールなら安全）。Microsoft Learn MCP（learn.microsoft.com/api/mcp）は匿名利用可でプリセット好適
- **2026-06-12 ホスト型エージェント（hosted-application/deployment）の実機ハマりどころ（AGT-04）**: ①OCIRの新規リポジトリはpush時自動作成が403 → `oci artifacts container repository create` で明示作成 ②環境変数typeはCLI生成例の`PLAIN_TEXT`でなく**`PLAINTEXT`**（`VAULT`も可、SDK enumが正） ③`inbound-auth-config`は省略不可かつ**IDCS_AUTH_CONFIG一択** ④deploymentのlifecycle-stateにCLI未知enum **NEEDS_ATTENTION** があり`UNKNOWN_ENUM_VALUE`表示になる → raw-requestで実値取得 ⑤イメージpullには動的グループ（resource.type=generativeaihostedapplication/deployment）+ `read repos` ポリシーが必要（無いとartifact FAILED）。手順: ops/deploy-hosted-agent.sh、詳細: docs/verification/agt-04.md
- 2026-06-12 podmanで `~/.oci` をコンテナにマウントする検証は **SELinuxラベルで読めず ConfigFileNotFound** になる → 検証用途は `--security-opt label=disable`（`:Z`はホスト側を関係ラベルに付け替えるため不可）。`key_file`が絶対パスなら同パスにもマウントする
- **2026-06-12 ホスト型アプリのinvoke URLは未文書・規則ベース**: applicationリソースのJSONにendpointフィールドは存在しない。実URLは `https://inference.generativeai.{region}.oci.oraclecloud.com/20251112/hostedApplications/{APP_OCID}/actions/invoke/{コンテナ側パス}`（`/actions/invoke` 以降がコンテナへパススルー。実測でFastAPIの404 JSONが返ることから確定）。認証はIDCS Bearer（アプリ設定のaudience/scopeとトークンのaud/scopeクレームが一致）。トークンなしは401、無効パスはコンテナの404が素通しで返る。port 8080で動作確認
- 2026-06-12 **動的グループの変更は反映に5〜10分かかる**（hosted-deploymentのイメージpullで実測: ルール追加直後の再試行は同エラー、8分待機後に成功）。IAM起因のFAILEDを「直後の再試行」で切り分けないこと。また**ポリシー文だけ追加しても動的グループのマッチングルールに該当リソースタイプが無ければ無意味**（今回の実ハマり）
- 2026-06-12 hosted-deploymentは**1アプリ=1本**（既存があると "already exists"、DELETING中も同様）。削除完了の判定はGETの404ではなく **lifecycle-state=DELETED**（DELETED後もGETは200を返し続ける）。createには `--compartment-id` 必須（"compartmentId is not available"）
- 2026-06-12 IDCSのOAuthリソース登録は audience末尾にセパレータが無くても機能する（audience=`jetuse-spike-agent`+scope=`invoke`→fqs=`jetuse-spike-agentinvoke` でトークンの aud/scope クレームは正しく分解された）。リソース+クライアント兼用の単一アプリ（is-o-auth-client + is-o-auth-resource + allowed-scopes fqs）でclient_credentials自給自足が可能
- 2026-06-12 **ACTIVEなhosted-deploymentは直接削除不可**（"Deployment deletion NOT allowed: ... is activeDeployment"。FAILED状態のものは削除可）。活性中の環境を畳むときは**hosted-application側をdeleteするとデプロイメントごとカスケード削除**される
- **2026-06-12 CSSコメント内に `--rw-*/` のようなワイルドカード表記を書くと `*/` がコメント終端と解釈され、直後のルールブロックが丸ごと無効化される**（UI-02のtheme.cssで実発生。`:root` 全体が落ちて全色喪失。ビルドもlintもエラーにならない）。CSS内のコメントで変数群を表すときは「--rw-系」のような表記にする
- 2026-06-12 **Oracle Linux 9では `npx playwright install chrome` が非対応**（"only Ubuntu and Debian are supported"）→ Google Chrome RPMを直接 `dnf install`（/opt/google/chrome/chrome に入り、Playwright MCPのchromeチャネルがそのまま見つける）。ヘッドレスで日本語/絵文字が豆腐になる場合は `google-noto-sans-cjk-ttc-fonts` + `google-noto-emoji-color-fonts` を入れてブラウザ再起動（UI-03）
- 2026-06-12 SPAはHashRouterのためPlaywrightでの画面遷移は `/#/path` 形式（`/path` 直打ちはホームが描画される）。認証必須設定を一時無効化するには `.env.local` を書き換えず `VITE_AUTH_REQUIRED=false npm run dev`（プロセス環境変数が.envより優先）
- 2026-06-12 Redwoodトークン抽出（UI-01）: oj-redwood.cssは**同名変数がライト→ダークの順で再定義**される（最初の定義=ライト）。`rgba(var(--色var), .6)` の入れ子が1箇所あり、varを実値解決すると `rgba(#hex, .6)` という不正CSSになるため正規化が必要。抽出スクリプト: packages/web/scripts/extract-redwood-tokens.mjs、レポート: docs/ui/tokens-report.md
- **2026-06-12 TTS `synthesize_speech` は `compartment_id` 必須**（SDKではオプショナル扱いだが、無いと404 NotAuthorizedOrNotFound が返る — 認可エラーが404に化けるので原因に気づきにくい）。Phoenix限定・`TtsOracleTts2NaturalModelDetails(voice_id, language_code="ja-JP")`（VOICE-01）
- **2026-06-12 MP3を単純連結（cat）した音声はバッチSTTが最初のストリームしか転写しない**（2本目以降が無言扱い。エラーにもならないので「短い結果」で気づくしかない）。複数発話の検証音声はTTSの `speech_settings.output_format="PCM"` でPCM取得→stdlib waveでWAV結合が確実（VOICE-01）
- 2026-06-12 バッチSTTの出力JSONオブジェクト名はジョブ依存（`{prefix}/job-xxx/...json`）のため、固定パスを組み立てず出力prefixで `list_objects` して特定する。ジョブは25秒音声で約10〜20秒で完了（VOICE-01）
- 2026-06-12 Whisperバッチの分かち書きトークンは話者区間ごとに結合し、ASCII語同士の境界のみスペースを残す後処理で自然な日本語になる（jetuse_core/minutes.py `_to_utterances`。「AP I」のような元データ由来の分割は残る）
- **2026-06-12 Speechバッチジョブは `manage ai-service-speech-family` だけでは動かない**: リソースプリンシパル（Container Instance）作成のジョブが一律 `INTERNAL_ERROR`（percent=0）でFAILED。原因はバケットレベル権限不足（公式要件は `manage object-family` + `read/inspect tag-namespaces`。既存 `manage objects` ではBUCKET系permissionが欠ける）。**認可不足が INTERNAL_ERROR・"Please retry" に化けて表示される**ため、ジョブの `created-by` がRPかユーザーかで切り分けるのが早い（VOICE-01）
- **2026-06-12 API GatewayはWebSocket非対応**（公式にHTTP/Sのみ。ルート定義にWSタイプ自体が無い）→ リアルタイムSTTのブラウザ経路は「音声=チャンクPOST/結果=SSE」中継で実装（docs/comparison/realtime-transport.md）。GW経由でも中継遅延は実測上無視できる（Whisperのfinal確定1〜3秒が支配項）
- 2026-06-12 リアルタイムSTT（oci-ai-speech-realtime）は**リソースプリンシパルでも動く**（`RealtimeSpeechClient(config={'region':...}, signer=rp_signer)`）。必要ポリシーは `manage ai-service-speech-family` のみ（バッチで必要なbuckets/tag-namespacesはストレージ非使用のため不要 — VOICE-02実測）
- **2026-06-12 TTS(synthesize_speech)のSDK既定出力はMP3ではなくWAV**（RIFF 24kHz PCM、1文272KB — Content-Typeを信じる前に `file` で確認）。`speech_settings=TtsOracleSpeechSettings(output_format="MP3")` 明示で約1/12（VOICE-03）
- 2026-06-12 **リソースプリンシパルのクロスリージョン呼び出しは追加IAM不要**（大阪CIからPhoenixのTTSを1.08秒で実行。IAMポリシーはリージョンスコープを持たない）。クライアントは `AIServiceSpeechClient({"region": "us-phoenix-1"}, signer=rp_signer)` でよい（VOICE-03）
- 2026-06-12 TTSボイスの全量は `list_voices`（Phoenix）で確認できる: **全75種・10言語、日本語はTTS_2_NATURALの5種のみ**（女性: Aiko/Hana/Sakura/Yuki、男性: Satoshi）。en-USは38種（HD含む）。リアルタイムSTTのORACLEネイティブモデル（partial・終端検出対応）は**日本語非対応** — 日本語の発話終端自動検知はクライアントVADで実装する（backlog #14）
- **2026-06-12 互換API画像入力の実機対応（MM-01）**: gemini-2.5-pro/flashとllama-3.2-90b-visionはchat completionsのcontent parts（`image_url`=data URI）でOK。**cohere.command-a-visionはCPモデル一覧に出るが互換APIで404**（オンデマンド提供なし — Grok系と同パターン）。**gpt-ossは画像付きリクエストを黙って受理するが画像は見えない**（受理≠機能、対照実験必須の好例）。なお `meta.llama-4-maverick/scout` がモデル一覧にACTIVE出現（ADR-0001時点から変化、未検証）
- 2026-06-12 ブラウザのみで動画E2Eを作る小技: canvas+`captureStream()`+MediaRecorderでwebmを生成し、`DataTransfer`でfile inputへ投入（Playwright検証でffmpeg不要 — MM-01）
- **2026-06-12 OpenAI Agents SDKをOCIで使う要点（FW-01/SPIKE-12）**: ①SDK既定のResponsesモデルは不可（SDKの簡易input形式をOCIの厳格スキーマが拒否）→ **`OpenAIChatCompletionsModel`を使えばfunction tool/handoffs/guardrails/streaming全部動く** ②`set_tracing_disabled(True)`必須（OpenAI本家へtracing送信を試みる） ③**gpt-oss-120bはchat completionsエンドポイントでも動く**（Responses系登録だが両対応）。ただしfunction calling引数に空キー等の崩れあり→スキーマ外キー除去の寛容化を入れる
- 2026-06-12 「Compartment ID must be provided」エラーは**ヘッダがあっても出る**: Responses系で `OpenAi-Project` ヘッダが無い場合の誤誘導メッセージ（実際の不足はProject）。ヘッダを足すと本当のエラー（input形式等）が見える
- **2026-06-12 Vector Storeの検索API(`vector_stores.search`)はDPホストにのみ存在**（CPホストは404）。file_search built-in(Responses限定)を使えないchat completions系エージェントでも、これをfunction tool化すればRAGが組める（FW-01b。OpenAi-Projectヘッダ必要）
- 2026-06-12 Agents SDKの承認フロー(HITL)はOCIでも動く: `FunctionTool.needs_approval=True`→`result.interruptions`→`to_state().to_json()`(6〜10KB)→HTTP往復→`RunState.from_json`→approve/reject→`Runner.run(agent, state)`で再開。ステートレスAPIでの承認往復が状態DBなしで実現できる（FW-01b）
- **2026-06-12 LangGraphもOCIで動く（FW-02/SPIKE-13）**: `ChatOpenAI(base_url=OCI互換, http_client=IAM署名httpx)` で基本/ツール/streaming/並列分岐グラフすべてOK。`create_react_agent`はv1非推奨(v2削除)→`langgraph>=1,<2`にpin。`StructuredTool`はdict JSONスキーマを`args_schema`に直接渡せる。ReAct系は検索結果に答えが無いと**同じツールを延々再試行して再帰上限に達する**ことがある → 上限到達時はon_tool_endで収集済みの結果からクローズ回答を生成する実装が有効
- **2026-06-12 ディスク逼迫時のpodman pushは「incomplete layer」警告つきで不完全に成功したように見える** → そのままterraform applyすると旧CIを壊した後にimage pull失敗で**API全滅**する（実発生、復旧15分）。デプロイ前に `df -h`（85%超なら旧イメージ掃除）と push出力の確認を必須にする。CI再作成方式は「壊してから作る」ため失敗時に止まる点に注意
- **2026-06-12 CrewAI 1.14をOCIで使う要点（FW-03/SPIKE-14）**: litellm非依存(openai SDK直)になっており、IAM署名は `LLM(interceptor=BaseInterceptor実装)` の `on_outbound` で注入。**`provider="openai"` 明示必須**（`openai/モデル名` 形式はOpenAI既知モデル名と照合され、不一致でlitellmフォールバック）。依存chromadbがsqlite3≥3.35要求 → OL9では `pysqlite3-binary` で `sys.modules["sqlite3"]` 差し替え
- 2026-06-12 **Vercel AI SDK(TS)もOCIで動く**: `createOpenAI({fetch: customFetch})` で `oci-common` の `DefaultRequestSigner` 署名を注入（generateText/streamText/tool callingまで確認）。Node 18+のfetchはボディ付きで `duplex: 'half'` 必要（SPIKE-14b）
- **2026-06-13 API GWのFunctionsバックエンドは専用IAMポリシー必須**（`ALLOW any-user to use functions-family ... where request.principal.type='ApiGateway'`）。無いとGWが一律500（functionの直接invokeは正常なので切り分けに注意）。また**GWルートの `{p*}` は「/api/presets」のような残りセグメント無しに一致しない** → 完全一致ルートの併設が必要（ARCH-02実測）
- **2026-06-13 cohere系はOpenAI互換APIで使えない**: chat completionsでも「Unsupported OpenAI operation」（safety_mode検証時に発覚）。cohereを使う場合はOCIネイティブSDK経路になる
- 2026-06-13 chat completionsストリーミングのusageは `stream_options={"include_usage": true}` でOCIも返す（監査のトークン記録に必須。responses系は既定で返る）
- **2026-06-13 OCIRへのpush失敗はexit 0で静かに起きうる**（ディスク逼迫時の「incomplete layer」警告が兆候）→ **apply前に `oci artifacts container image list` でレジストリ側にバージョンが存在するか確認する手順を必須化**（今回これで2度目の全滅を未然防止）
- **2026-06-13 OCI Logging/Monitoringへのアプリ直接書き込みは専用IAM必須**: PutLogs(カスタムログ ingestion)に `use log-content`、PostMetricData(カスタムメトリクス)に `use metrics`。無いとサービスログ(GW/Fn invoke)は出るがアプリのカスタムログ・メトリクスだけ0件になる(切り分け注意)。送信はベストエフォート非同期(失敗してもリクエストをブロックしない)実装が安全。logging-searchのクエリ対象は `"compartmentOCID/logGroupOCID/logOCID"` のパス形式(OCID単体だとNotAuthorizedOrNotFound)
- **2026-06-13 Terraformで環境変数注入のためCIを再作成しても、イメージタグが同じだとコード変更は反映されない**（OPS-02でobs.py追加後、LOG_OCID注入のapplyが0.26.0を再利用→新コード不在で送信されず）。コード追加を伴う変更は必ず新イメージのビルド・push・tfvarsのタグ更新をセットで行う。**CIのstderr確認は `oci container-instances container retrieve-logs --container-id <id> --file -`**（CIにサービスログを付けていなくても取得できる。デプロイ済みコードの実体確認に有効）
- 2026-06-13 OCI Loggingのカスタムログにアプリから直接ingestしたエントリの `source` は、CI内では送信側が付けた値かコンテナのhostname(=localhost)になる。検索は `search "compartment/loggroup/log"` パス形式。索引化は2〜4分。Monitoringカスタムメトリクスは `metric list` で定義の存在、`summarize-metrics-data` でdatapoint値を確認できる(集計は直近データだと数分遅延で空に見えることがある)
- **2026-06-13 OCI大阪リージョンには画像生成・動画生成モデルが無い**（`oci generative-ai model-collection list-models` の提供capabilityは CHAT/FINE_TUNE/TEXT_EMBEDDINGS/TEXT_RERANK のみ。"image"を含むモデルはマルチモーダル埋め込みかvision入力チャットで生成系ではない）。JetUseの画像生成/動画生成ユースケースはOCI版では当面実現不可（プラットフォーム制約。他リージョン/将来提供は要再確認）
- **2026-06-13 OCIマネージド・ガードレール(ApplyGuardrails)の実効範囲**: プロンプトインジェクション検知=言語非依存で機能(0/1スコア、0.16-0.31s高速、オンデマンド可) / コンテンツモデレーション=**日本語非対応(`Language ja is not supported` 400、英語は1.0で機能)** / PII=デフォルト設定では未検知(null)。ネイティブSDK(generative_ai_inference.apply_guardrails)経由、IAMは use generative-ai-family でカバー。日本語アプリではプロンプトインジェクションのみ採用可(GAP-01)
- **2026-06-14 Agents SDKはLLMが存在しないツール名を呼ぶと `ModelBehaviorError`("Tool X not found in agent")でrun全体が中断する**（gpt-ossが幻覚ツール名 例:web_find を出して実発生）。対策: MaxTurnsExceeded同様に捕捉し、`exc.run_data`(AgentsExceptionに付与される収集済みitems)を使ってツールなしのクローズ実行で最終回答を生成(jetuse_core/agents_sdk.py `_closing_answer`)。実機で複数ツール調べ物タスクが完走することを確認
- **2026-06-15 OCIにspeech-to-speech(双方向音声)モデルは無い（GAP-05/SPIKE-G5で確定）**: モデル能力一覧が CHAT/FINE_TUNE/TEXT_EMBEDDINGS/TEXT_RERANK のみで AUDIO 系ゼロ。OpenAI互換APIの面にも realtime audio/audio エンドポイント無し。**Nova Sonic相当の全二重音声はマネージドで実現不可**（プラットフォーム制約）。STT(partial無し)+LLM+TTS(Phoenix往復)のパイプラインで作れるのは「連続半二重(VAD自動送信+簡易バージイン)」が上限で、これは新マネージド能力ではなくフロント実装タスク（SPIKE-06の制約に同じ）
- **2026-06-15 マネージドのコード実行はOCI Responses built-in(`code_interpreter`)のみ＝nativeエンジン専用（GAP-03/SPIKE-G3で確定）**: chat completions経路(Agents SDK/LangGraph)にはbuilt-in toolが無く(ADR-0007)、SDKのResponses直結も入力形式不整合で不可。自前サンドボックス(別コンテナ/gVisor/Functions使い捨て)は任意コード実行で**マネージド軸外＋安全性リスク**のため不採用。**「コード実行が必要ならnativeエンジンを選ぶ」をエンジン使い分けの正式ルールに確定**（欠落ではなくプラットフォーム制約による設計判断）
- **2026-06-15 Google ADK を OCI で動かすにはカスタムBaseLlm(SPIKE-ADK)**: ADKの`LiteLlm`は静的kwargsのみでOCIのリクエスト毎IAM署名に非対応 → `BaseLlm`を継承し、既存の署名済み`OpenAI`クライアントでchat completionsを叩く方式でReAct(ツール呼び出し→結果→回答)が完走。**ADK 2.2.0はPython3.12必須**(3.9は1.18.0止まり)。翻訳: LlmRequest(genai types: Content/Part/FunctionCall/FunctionResponse, config.tools=function_declarations)↔OpenAI messages/tools(function_response→role:"tool",tool_call_id一致)。`google-adk[extensions]`が既定importに必要。応答にusage無く警告(無害)。3SDK(Agents SDK/ADK/LangGraph)すべてOCIでReAct可能と確定
- **2026-06-15 OCI Hosted Applicationのコンテナは8080でlisten必須(AGT-MULTI)**: ポート8000で公開したらデプロイが11分CREATING後にNEEDS_ATTENTION/work-request「timed out before the container was ready to serve requests」で失敗。AGT-04サンプルは8080。コンテナはローカルでは正常起動(/health 200)するので切り分け注意。Containerfileを `EXPOSE 8080`+`uvicorn --port 8080` に修正で解決。work-requestエラーは `GET /20231130/workRequests/{id}/errors`(raw-request)で取得できる
- **2026-06-15 OCI Hosted Applicationのイメージ更新は「アプリ削除→再作成」が確実(AGT-MULTI)**: ①アクティブなデプロイメントは直接削除不可(`is activeDeployment` 403) ②`hosted-deployment update --active-artifact` は登録済みartifact間の切替のみで**新タグは追加できない**(`Artifact not exists`) ③`hosted-application update --environment-variables` 直後はアプリがUPDATINGになり、ACTIVE復帰前にdeployment操作すると409。**確実なのはアプリをdelete(=deploymentもカスケード削除される)→新イメージ+フルenvで作り直し**(APP OCIDは変わるのでtfvars更新+API再デプロイ要)。
- **2026-06-15 meta.llama-3.2-90b-vision は1リクエスト1画像まで(ENH-09実機)**: 複数画像を送ると `400 "At most 1 image(s) may be provided in one request"`。**gemini-2.5-pro/flash は複数画像OK**(6枚で実機確認)。対策: モデルに `multi_image` フラグを追加し、映像分析(複数フレーム)はmulti_image対応モデルのみ提示、チャットの画像添付は単一画像モデルでは1枚に制限。
- **2026-06-15 ADB 26ai Select AI Agent(DBMS_CLOUD_AI_AGENT)の実機API(SPIKE-E1)**: ①`GRANT EXECUTE ON DBMS_CLOUD_AI_AGENT TO JETUSE_APP` 必須(無いとPLS-00201。JETUSE_APPはDBMS_CLOUD_AIのみ保有していた) ②CREATE_AGENT attributes={profile_name,role} / CREATE_TASK={instruction}(agent属性は不可ORA-20051) / CREATE_TEAM={agents:[{name,task}],process:"sequential"}(他process値はORA-20053) ③実行は `SELECT DBMS_CLOUD_AI_AGENT.RUN_TEAM(team_name=>,user_prompt=>) FROM dual` で、**事前に CREATE_CONVERSATION+SET_CONVERSATION_ID 必須**(無いとCONVERSATION_ID# NULLでORA-01400) ④CREATE_TOOL未設定だとエージェントは反射応答のみ(実データ回答にはSQL/RAGツール登録が要る) ⑤チーム作成は内部で `AGENT$<team>` というOracle-maintainedプロファイルを作り、同名再作成はORA-20046 → チーム名は一意/使い捨て
- **2026-06-15 Select AI Agent の RUN_TEAM は既定のcall_timeout(10s)では足りずDPY-4024(ENH-04)**: db.connect()は全接続に call_timeout=10000ms を設定する(CHAT-07)。RUN_TEAMはLLMでNL→SQL→実行を多段実行するため10sを超え DPY-4024 "call timeout exceeded" になる。select_ai_agent.run側で `conn.call_timeout=240_000` に引き上げて解決(SSEはmain.pyのkeepaliveで継続、GW readTimeout 300s内)。nl2sqlのSelect AI生成も同様に60_000へ上げている。
- **2026-06-15 Select AI Agentの一覧系質問はHTTP 413(ORA-20413)/JSON崩れ(ORA-40441)になりやすい(ENH-04)**: エージェントがSQL実行結果を次のLLM呼び出しに渡すため、全件SELECT等で結果が巨大だと inference /actions/chat が 413。対策: エージェントのrole(指示)に「一覧系は最大50行・全件返却しない/集計はそのまま」のガードを付与し、それでも失敗するジョブ(ORA-20053系=413やLLMのJSON崩れORA-40441)は生ORAを出さず「集計・上位N件に言い換えて」という日本語ガイダンスへ変換(PayloadTooLargeError)。集計/上位N件の質問は安定。
- **2026-06-15 翻訳は OCI Language / Enterprise AI LLM の2択(ENH-10/SPIKE-E5)**: どちらもap-osaka-1可用・低レイテンシ(JP→EN: LLM 0.2-0.5s/文、OCI Language 0.12s/文)。OCI Language(ai_language.batch_language_translation)は翻訳特化で最速だが**CIのRPに `use ai-service-language-family` のIAMが必要**。LLM(llama-3.3-70b)は既存generative-ai権限で動くため既定に採用。
- **2026-06-16 OCI Language方式はCI実機で404 NotAuthorizedOrNotFound・ローカルでは成功(ENH-10)**: SPIKE-E5はローカル(~/.ociユーザー認証)で検証したため通っていたが、本番CI(リソースプリンシパル)では `use ai-service-language-family` 未付与のため `batch_language_translation` が404で失敗。ユーザーには「翻訳が機能していない」に見えた。教訓: マネージドAIサービスはローカルのユーザー権限とCIのRP権限が別物 → 実機CIで必ず再検証。対策として `translate()` は ServiceError等を捕捉して自動的にLLM方式へフォールバック(CIログに `falling back to llm` 警告)。IAM付与後はoci_language方式が選択時そのまま機能。診断はCIログ取得(`oci container-instances container retrieve-logs`)が決め手。
- **2026-06-16 CIを `-target` で再作成するとAPI Gatewayのバックエンドが旧プライベートIPを指したまま取り残される(全リクエスト到達不能=curl 000)**: Container Instanceは再作成のたびにVNICのプライベートIPが変わる(例 10.1.1.190→10.1.1.161)。`terraform apply -target=...container_instance.this` だけだとゲートウェイのdeployment(`...api/${request.path[p]}` 等のbackend url)は更新されず、GWがバックエンドに繋がらず全リクエストが落ちる(curlで `000`)。教訓: **イメージ更新でCIを再作成したら必ずフルの `terraform apply`(ノーターゲット)でゲートウェイのbackend IPまで反映する**(plan上は deployment in-place 1件のみ・破壊なし)。切り分け: `000`=GW→backend到達不可(IP不一致), `401/404`=appまで到達(=疎通OK)。なお3エージェントのHosted ApplicationはOCID参照のためIP変化の影響を受けない。
- **2026-06-16 OCI Document Understanding(OCR)は大阪可用・日本語高精度・同期APIで実用(ENH-07/SPIKE-E4)**: `analyze_document`(同期/inline base64)で画像/PDFをそのまま渡せる(多ページ/大量は非同期 create_processor_job)。日本語OCRは char recall 100%/mean conf 0.994(1ページ6.9s)。**テーブル抽出は罫線ベース**で、罫線なしの空白整列は表として検出されない(セル文字はTEXT抽出で取得可)。言語コードはISO639-2/B系(JPN/ENG/CHI_SIM/KOR等)。IAMは翻訳同型で `use ai-service-document-family` がCIのRPに必要(未付与だと404、`docunderstand.ocr()`が422の友好メッセージへ変換)。
- **2026-06-16 Document Understanding 同期API(analyze_document/inline)は最大5ページ固定(ENH-07/SPIKE-E4)**: 実測で6ページ以上は `HTTP 413: Input file has too many pages, maximum number of pages allowed is: 5`。3/5ページはOK(5ページ5.9s)。これはOCIサービス側の固定上限でアプリ側設定ではない。6ページ超は非同期 create_processor_job(Object Storage入出力・公式上は数千ページ)が必要。docunderstand.ocr()は413を「最大5ページ。分割を」の日本語へ変換、UIにも上限明記。
- **2026-06-16 OCRの5ページ上限はサーバー側分割で回避(ENH-07b)**: 同期API(analyze_document)の5ページ上限に対し、pypdfでPDFを5ページ単位に分割→各チャンクを同期OCR→lines/tables/kv/confidenceをマージする方式で5ページ超に透過対応(12ページ=3チャンクで全ページ抽出を実機確認、mean conf 0.99)。非同期 processor job は入出力バケット/ポーリング/追加IAMが要るため、入力規模が中規模(〜100ページ)のプロトタイプ用途では分割方式を採用。画像/5ページ以下PDFは分割せず単発。制限と回避の総まとめは docs/guides/ocr-limits-and-workarounds.md。
- **2026-06-17 多ページOCRが504になる=GWの汎用ルートread_timeout60秒×同期ブロッキング(ENH-07d)**: OCRは結果まで何も返さない同期POSTで、SSEのチャット専用ルート(300s)と違い `/api/{p*}` は read_timeout=60s。チャンク直列だと60s超で504。対策=①チャンクを並列OCR(ThreadPoolExecutor、ページ順維持)で壁時計短縮 ②`/api/ocr`専用ルートをread_timeout=300で追加(完全一致ルート必須。{p*}は末尾セグメント無しに不一致)。注: API GatewayのHTTP_BACKENDはconnect/send timeoutが>=1必須で、ルート挿入で状態indexがFunctionsルート(0)とずれると400 → HTTP_BACKENDではconnect=60/send=300を明示。実機: 14ページ=200/10.4秒で解消。
- **2026-06-17 Document Understanding のテーブル抽出は日本語非対応(英語系のみ)(ENH-07/SPIKE-E4)**: 「PDFが表でなく文字起こしになる」のは精度問題ではなく言語制限。同一罫線表で内容だけ変えた対照実験で、英語=6×4正確に構造抽出、日本語(language=JPN/自動)=テーブル0件(テキストのみ)、日本語をlanguage=ENGで無理やり通すと表は出るがセル文字が文字化け。TEXT抽出は日本語高精度だがTABLE構造抽出は英語系のみ。回避は①OCR本文をLLMでMarkdown表へ再構成(日本語可・要hallucination注意明示) ②language=ENG時のみネイティブ表抽出。詳細 docs/guides/ocr-limits-and-workarounds.md §1b。
- **2026-06-17 OCRテーブル抽出のリージョン差なし+ヘッダー欠落バグ(ENH-07e)**: 同一表をOsaka/Phoenix/Chicago/Ashburnに投げ、英語=全リージョン検出・日本語=全リージョン0件を実機確認(リージョン変更では日本語非対応は解決しない)。また英語でも表が崩れて見えたのはアプリ側バグ=OCIはヘッダーを `header_rows` に返すのに `body_rows` しか読まずヘッダー行が空になっていた→ header_rows+body_rows+footer_rows を順展開して修正(GAN論文Table1でヘッダー復元確認)。UIは「ネイティブ機能の範囲に合わせる」方針で表抽出チェックを language=ENG 時のみ表示。残存癖: ±→+(OCI文字認識側)。
- **2026-06-17 OCRエンジンをDocument Understanding/VLMの選択式に(ENH-07g)**: 日本語の表抽出はDUが非対応のため、ビジョンLLM(gemini-2.5-pro/flash)でページ画像を読むVLMエンジンを追加。PDFはpymupdfで画像化→ページ毎にLLM(並列)→JSON({text,tables})で受けて構造化。実機(GW経由)で日本語罫線表PDF=HTTP200/13秒・ヘッダー+全行を正確抽出(±記号も保持)。トレードオフ=ページ毎LLM呼び出し(コスト・時間増)・厳密OCRでない・要pymupdf。DUは日本語テキスト高精度/表は英語のみ/KVあり、VLMは日本語表可/KVなし、と棲み分け。
- **2026-06-17 OpenSearch RAGは大阪可用だが常設クラスタ課金が論点(ENH-05/SPIKE-E2)**: OCI Search with OpenSearchはap-osaka-1可用(cluster list成功)。ただしmaster+data+dashboardの常時稼働マルチノードで、アイドルでもOCPU/メモリ/ストレージが継続課金(最小~$100-150/月、HAで数百ドル/月)。ADBのような夜間停止運用は基本不可。既存のFile Search(実質サーバレス)/Select AI RAG(稼働中ADB再利用)は増分常設費なしなので、ハイブリッド(BM25+kNN)/全文検索/大規模/関連度チューニングが要件化するまでは常設費に見合わず既定非採用が妥当。採用時はk-NNプラグイン+OCI GenAI埋め込み自前投入、プライベートサブネットのREST(basic認証)。go/no-go詳細: docs/verification/SPIKE-E2.md。
- **2026-06-17 OpenSearch RAGを第3バックエンドとして実装(ENH-05、ユーザー承認で常設)**: 最小クラスタ(master1/data1/dashboard1・2.19.1)をTerraform(modules/opensearch・enable_opensearch)で常設。rag_opensearch.pyでチャンク→embed_text→k-NN index→検索→llama-3.3-70b回答。/ragで選択可、アップロードは3バックエンド同時取り込み。E2E成功(GW経由)。ハマり所2つ: ①**security_mode=DISABLEDでも9200はTLS** → 平文HTTPだと「Server disconnected without sending a response」。https+verify=False(私設subnet・証明書CN不一致)で解決 ②**OpenAI互換APIは/embeddings非対応(400 Unsupported OpenAI operation)** → ネイティブ generative_ai_inference.embed_text(cohere.embed-multilingual-v3.0/1024次元)を使用。クラスタ最小構成でも master memory 16GBは下限割れ(32GBへ)。CLIのcluster作成は --nsg-id でNSG指定可。
- **2026-06-17 RAGの取り込み状況をバックエンド別に可視化(ENH-05追補)**: アップロードが各バックエンドに反映済みか /api/rag/files の backends={vector_store,select_ai,opensearch} で返す。判定方法: VS=Files APIの処理状態、OpenSearch=indexのfile_id集約(同期=即時)、**Select AI=ベクトル索引 {INDEX}$VECTAB の attributes.object_name("{file_id}_{filename}") に存在するか**。Select AIはrefresh_rate(既定60分)間隔のバケット同期なので最も反映が遅く、UIで⏳(pending)と明示。$VECTABの列はCONTENT(CLOB)/ATTRIBUTES(JSON)/EMBEDDING(VECTOR)。UIは各ファイル右にVS/SAI/OSバッジ、pending中はゆっくりポーリング(20秒)。
- **2026-06-17 Oracle Trusted Answer Searchの正体と当ADB未提供(ENH-06/SPIKE-E3)**: TAS=Oracle AI Database 26aiの「自然言語→精選ターゲット(レポート/URL/SQLビュー)」決定的マッピング機能。**LLMを使わず**AI Vector Searchで意味一致し事前承認済み結果を返す(ハルシネーションなし)。提供3経路=Admin APEXアプリ/Portal APEXアプリ/Search API。Search API=PL/SQL `DBMS_TRUSTED_SEARCH.SEARCH()`(NL質問→構造化JSON)。構成=Search Space/Search Target/Target Action/Target Inputs(:QUARTER等)/Target Value Sets。**実機判定: jetuse-dev-adb(23.26.2.2.0=26ai)にDBMS_TRUSTED_SEARCHは存在せず**(DBMS_CLOUD_AI/AGENTはあり)→ADB Serverlessに未提供でそのままは適用不可=A項目(no-go)。本質はベクトル一致の決定的ルーティングなのでAI Vector Search+embeddingsで自前実装は可能だが別物。詳細: docs/verification/SPIKE-E3.md。
- **2026-06-18 DBチャットのAIサンプルデータ生成(feedback 20260618-3)**: 「どんなデータか」の説明文だけからLLMにCSVを生成させ、既存のCSV取り込み(datasets.create_dataset)に流して即DBチャット対象化する機能を追加。実機ハマり2点 ①**create_datasetのVARCHAR2幅見積もりが文字数基準でORA-12899**: Oracle既定はBYTEセマンティクスのため日本語値(UTF-8で1文字3バイト)が「VARCHAR2(16)に18バイト」で溢れる。`len(v.encode('utf-8'))*2`(最小32)に修正(CSVアップロード経路の既存バグも併せて解消) ②**列名のASCII化はモデル依存**: llama-3.3-70bは「列名は半角英小文字スネークケース」指示を無視して日本語ヘッダを出し、_ident()が全非ASCII→C/C_1...の無意味名に潰れる。gemini-2.5-flashは指示遵守でEMPLOYEE_NAME/MONTHLY_SALARY等の意味のある列名を生成→NL2SQL(Select AI)が「部署ごとの平均月給」等を正しくSQL化・実行できることをE2E確認。生成は GEN_MODEL=gemini-2.5-flash 採用。
- **2026-06-18 開発者ごとのデプロイ済みE2E環境(複数人開発)**: 高価な常設リソース(ADB/Identity Domain/VCN/OCIR)は共有し、開発者ごとに分けるのはアプリ層(Container Instance+API Gateway+SPAバケット)とADB専用スキーマだけ、という構成を追加(`docs/guides/dev-environments.md`)。要点: ①既存 `environments/dev` は state移動せず正本のまま、新 `environments/app` が `terraform_remote_state`(localバックエンド, ../dev/terraform.tfstate)で共有OCIDを参照する追加のみの構成。②per-devゲートウェイは `functions_routes={}` で全 `/api` を本人CIへ流す(Functions不要)。③IAM動的グループはリソース種別+コンパートメントで照合するため、同一コンパートメントに作る限り新CIは自動で権限継承(DG/ポリシー増設不要)。④認証は既定 `AUTH_REQUIRED=false`＋専用スキーマで分離=ゲートウェイ毎のOIDCリダイレクトURI登録(人手・承認制)を回避、公開GWは `apigw_allow_cidr` でIP制限。⑤DBスキーマは `settings.adb_user`/`adb_query_user`(env `ADB_USER`/`ADB_QUERY_USER`)で可変化(datasets.py/nl2sql.py のハードコード除去)。⑥ops: `setup-dev-schema.py`(スキーマ作成+権限+migrate)/`dev-env-up.sh`(build/push→plan確認→apply→SPA配信)/`dev-env-down.sh`/`dev-env-stop.sh`(CI停止で課金停止)。注意: イメージ更新=CI再作成=GW再デプロイのためタグは不変shaを固定。共有devは出力追加分を反映するため一度apply(リソース変更0)が必要。
- **2026-06-18 agent.framework のレガシー値が実機DBに残存・移行が必要(refactoring P0.6/ADR-0010)**: 入力(API)は現行Literal(`openai_agents`/`adk`/`langgraph`/`select_ai`)のみ受理しレガシー値を422で拒否するが、**DB側には旧値が実在**した。dev ADB(JETUSE_APP)の `SELECT framework,COUNT(*) FROM agents GROUP BY framework` 実測で `agents_sdk`:2 / `native`:2(`010_agent_framework.sql` の `DEFAULT 'native'` 由来)が残存。移行 `012_normalize_framework.sql`(`UPDATE agents SET framework='openai_agents' WHERE framework IN ('native','agents_sdk','hosted')`・冪等)で canonical 化(適用後 openai_agents:8/adk:1/select_ai:1)。read-time `hosted_agent.normalize_sdk()`/`_LEGACY_SDK` は現役の正本で**削除対象外**(P0.7のin-processエンジン削除とは別物)。
- **2026-06-18 dev ADBへ手動でSQL/マイグレーションを流す接続レシピ**: `.env` のキー(`ADB_APP_PASSWORD`/`ADB_ADMIN_PASSWORD`/`ADB_QUERY_PASSWORD`)は pydantic `Settings`(env名=フィールド名大文字、別名なし `extra=ignore`)の `adb_password`/`adb_dsn` 等に**直接はマップされない**。手動接続は oracledb で `user='JETUSE_APP', password=ENV['ADB_APP_PASSWORD'], dsn='jetusedev_low', config_dir=/tmp/jetusedev_wallet, wallet_location=同, wallet_password=tfvars(ADB_WALLET_PASSWORD)`。スキーマ横断調査は ADMIN(`ADB_ADMIN_PASSWORD`)で `all_tables`/`<owner>.agents` を参照。`migrate.migrate()` をスクリプトから呼ぶ場合は `os.environ['ADB_USER'/'ADB_PASSWORD'/'ADB_DSN'/'ADB_WALLET_DIR'/'ADB_WALLET_PASSWORD']` を設定してから import すると settings が拾う。ADB夜間停止のため作業前に `bash ops/start-adb-if-stopped.sh`(AVAILABLE化に数分)。
- **2026-07-13 新規作成直後の GenerativeAiProject は DP から数分見えない(FIX-47 E2E)**: CP で ACTIVE になっても、その OCID を `OpenAi-Project` に載せた DP 呼び出し(files.list 等)は数分間 404 を返す(実測 2〜3分。vector store の CP→DP 伝播と同パターン)。クリーンルーム初回リクエストは project 自動作成→即 DP 呼び出しでこの窓に当たる → 一時的な 503(ヒント付き)は仕様として許容し、`GET /api/rag/health` が ok になってから再試行が正。伝播をコードで待ち込むとチャット経路が分単位でブロックされるため採用しない。
- **2026-07-13 ADB を prefix 変更で in-place rename すると wallet が stale になり DB 全断(FIX-47 E2E)**: `oci_database_autonomous_database` の db_name 変更は置換でなく in-place 更新で、tnsnames のサービス名(`<dbname>_low`)は変わるのに `oci_database_autonomous_database_wallet` は `autonomous_database_id` 不変のため再生成されない → 旧サービス名のウォレットがバケットに配置され `DPY-4000` で接続不能。復旧=Database API でウォレット再生成→バケット上書き→CI 再起動。恒久修正は wallet リソースに `lifecycle.replace_triggered_by = [db_name]`(PORT-01)。またアプリの `/tmp/adb_wallet` キャッシュは tnsnames 不整合を検知せず再フェッチしないため、復旧には CI 再起動が必須(PORT-02)。
- **2026-07-13 ORM の on-behalf-of 実行はテナンシレベル読取に明示 policy が要る(FIX-47 E2E)**: `data.oci_identity_region_subscriptions` は直接 CLI(ユーザー認証)では暗黙に読めるが、RM ジョブの OBO トークンでは `inspect tenancies in tenancy` が無いと **エラーでなく null** を返し、home リージョン導出(providers.tf)と deploy_region_key(Issue #55)が `Iteration over null value` / region_guard 失敗で plan 不能になる。deployer policy には既に `inspect tenancies` が含まれるが、含まないグループの実行では再現する。切り分け: ローカル plan は通るのに RM だけ落ちたらこれ(PORT-01)。あわせて prefix は15文字以内でないと VCN dnsLabel 上限(15)で apply が落ちる(schema.yaml に長さ検証なし — PORT-01)。
- **2026-07-13 ORMワンクリックの対応リージョンは2階層(アプリ4 / GenAI検証済2)(PORT-01)**: コンテナイメージは公開OCIRの4リージョン(kix大阪/nrt東京/iadアシュバーン/ordシカゴ)へ事前pushしてあり、region_guard(main.tf)はこの4つ+イメージミラー指定を許す。だが**GenAI(推論+agentic API)の実証済は大阪(kix)とシカゴ(ord)のみ**で、nrt/iadは apply が綺麗に通っても RAG/会話メモリ/デモ生成が全滅する(推論エンドポイント非提供)。よって region_guard に GenAI 用の第2 precondition を追加し、未検証リージョンは `allow_unvalidated_genai_region=true` の明示オプトインを要求する(既定は kix/ord のみ許可)。イメージのミラーでは回避できない(GenAI自体が無い)ため OCIR ガードとは別条件。東京/アシュバーンでのGenAI提供開始は要再確認。
- **2026-07-13 `oci.config.from_file()` 直呼びは genai系の入口(`_signer()`/`OciUserPrincipalAuth`)にも潜む(PORT-02 AUTH_MODEガード)**: AUTH_MODE未設定コンテナでの `~/.oci/config` 不在ガードは、素朴に `grep oci.config.from_file` した14箇所(db/rag/obs/tts/stt_realtime/docunderstand/minutes/guardrails/embeddings/mcp_servers/agents/translate等)を共通ヘルパ化するだけでは不十分。`oci_genai_auth.OciUserPrincipalAuth()`(genai.py `_signer()` が呼ぶ、make_inference_client/make_cp_client/nl2sql等genai全経路の認証入口)は**内部で独自に** `oci.config.from_file()` を呼ぶため、この呼び出しはgrep対象に映らずヒントなしの生 `ConfigFileNotFound` が漏れる。教訓: サードパーティラッパー(OciUserPrincipalAuth等)が内部で設定ロードするケースは、ラッパーの呼び出し側(`_signer()`自体)を try/except で包む必要がある。
- **2026-07-13 dbchatのbackend選択はweb UIが常に明示送信するため「未指定」を表現できない(PORT-02 dbchat既定切替)**: `Nl2SqlRequest.backend` を `Literal[...] | None = None` にして「未指定=サーバー既定に任せる」を導入し、明示 `sql_search` はSemanticStore未構成でも強制(ヒント付きエラー)という設計にしたところ、codex-reviewで**blocker**判定(`packages/web/src/pages/dbchat.tsx` の `useState<Nl2SqlBackend>('sql_search')` が常に明示送信するため、SEMSTORE_OCID未設定環境で既定のdbchat縮退が実UIから到達不能になっていた)。対象area外(packages/web)への変更を避けるため `backend="sql_search"` は明示/既定を区別せず一律select_aiへ自動切替する設計に戻し、trade-off(SQL-04比較モードがSEMSTORE_OCID未設定環境で両パネルselect_aiになりうる)を`ponytail:`コメントで明記。教訓: APIのみで「既定値」を区別可能にするスキーマ変更は、既存フロントの初期state送信パターンを先に確認しないと実到達性を壊す。
- **2026-07-13 OCI Functionsルーター(fn/router/func.py)はADR-0005で二重実装禁止だが機械的に検出できない(PORT-02)**: `/api/dbchat/schema` と `/api/tts` はFastAPI(service/routes/)とFunctionsルーター(fn/router/func.py)の両方に実装があり、後者はCI/gitのlint/testでは一切実行されないパス(呼び出しはAPI GWのfunctions_routes経由)。FastAPI側だけ縮退メッセージ/新フィールドを追加してもruff/pytestはグリーンのまま通り、codex-reviewの手動diff読解で初めて発覚した。教訓: service/routes配下のエンドポイントを変更するときは必ず `grep -rn "<同一path>" packages/api/fn/` で二重実装の有無を確認する。
- **2026-07-28 agentic API(Responses/Conversations)は `generative-ai-family` に含まれない独立 resource-type(FIX-58 / DEPLOYTEST 実機)**: クリーンなテナンシ(us-chicago-1・ホーム ca-toronto-1)へ公開ORMスタックをデプロイすると、`POST /openai/v1/responses` と `/conversations` が **resource principal のときだけ 404** になり、既定チャットモデル(gpt-oss-120b=responses系)・RAGの引用付き回答・会話メモリが揃って死ぬ。同じ compartment・同じヘッダでもテナンシ管理者のユーザープリンシパルでは通るため「リージョン非対応」と誤診しやすい(実際 `oci generative-ai model-collection list-models` には gpt-oss-120b が居るし chat/completions は動く)。原因は runtime policy に `generative-ai-response` / `generative-ai-conversation` が無いこと。**有効な resource-type かどうかは policy を1文だけ作って確かめられる**: 無効なら `CreatePolicy` が 400 "No permissions found"、有効なら(非ホームリージョンでは)"Please go to your home region ... to execute CREATE" まで進む。この方法で `generative-ai-response`/`-conversation`/`-chat`/`-model` は実在、`generative-ai-responses`(複数形)/`generative-ai-agent-family` は非実在と確認。付与後 IAM 反映まで実測 3〜5分。
- **2026-07-28 Identity Domains は管理者が SCIM の User へ password を書くと必ず mustChange=true になる(FIX-58)**: ワンクリック配備のデモユーザーは `oci_identity_domains_user.password` で作っていたため、初回サインインが `/ui/v1/pwdmustchange` に飛び、スタック出力の `demo_password` ではアプリに入れなかった(=「デプロイしたのにログインできない」)。`passwordState.mustChange` は **readOnly** で PATCH しても無視され、作成時に `--ext-password-state-user '{"mustChange":false}'` を渡しても無視される。唯一効くのは **UserPasswordChanger**(`PUT /admin/v1/UserPasswordChanger/{id}`)で、これで設定すると mustChange=false になる。Terraform provider に該当リソースが無いため、ユーザーは password 無しで作り、`terraform_data` + local-exec で `oci identity-domains user-password-changer put --force` を呼ぶ。**Resource Manager の実行環境には OCI CLI(3.68.0)が委任トークンで認証済みの状態で同梱されている**(`OCI_CLI_AUTH`/`OCI_DELEGATION_TOKEN_FILE` が設定済み)ので、ボタン経由のデプロイでも追加作業なしで動く(local-exec 実測3秒)。なお同じユーザーへ同じパスワードを再設定すると `error.identity.passwordmgmt.pwdpolicyViolation`(履歴4世代)になるため、作成時パスワードは設定しないほうが素直。
- **2026-07-28 OCI CLI の書き込み系は非対話環境で `--force` が要る(FIX-58)**: `oci identity-domains user patch` / `app patch` / `user-password-changer put` は "Updates to schemas and meta ... Are you sure you want to continue? [y/N]" を出す。Terraform の local-exec や CI では stdin が無く Abort する。destroy 時にOIDCアプリを非アクティブ化する provisioner も同様で、`--force` が無いと teardown が失敗する。
- **2026-07-28 API Gateway の汎用ルート60秒はRAG初回アップロードを必ず落とす(FIX-58)**: 新規デプロイ直後の1件目は Vector Store の CP→DP 伝播待ちを含み実測 66.7秒(アプリ側は 200 で完了)。`/api/{p*}` の read_timeout=60 では**ゲートウェイだけが 504 を返し**、UIには「⚠ HTTP 504」、コンテナログには成功が残るという食い違いになる。OCR(`/api/ocr`)と同じく `/api/rag/files` と `/api/rag/{p*}` を read_timeout=300 の専用ルートにする(完全一致ルート併設は必須 — `{p*}` は末尾セグメント無しに一致しない)。
- **2026-07-28 bootstrap は別プロセスなので health のプロセス内グローバルは届かない(FIX-58)**: `entrypoint.sh` は `python -m jetuse_core.bootstrap &` で**別プロセス**として起動する。`_set_resource_principal_status()` が立てるモジュールグローバルは uvicorn 側から見えず、Select AI が実際には動いていても `/api/health` の `dbchat` が恒久的に `unavailable`(`select_ai.ok=null`)になる。実機では NL2SQL が正しく SQL を生成・実行できるのに health だけ赤い、という誤診断を招いた。同一コンテナのファイル(`/tmp/jetuse-rp-status.json`、env `RP_STATUS_FILE`)経由で受け渡す。
- **2026-07-28 DBブートストラップはコンテナ再起動で必ず ORA-28007 に当たり、25分間「ADB 未準備」と誤表示して migrate ごと落ちる(FIX-58)**: `_ensure_user()` は `CREATE USER` が ORA-01920(既存)なら `ALTER USER ... IDENTIFIED BY <同じパスワード>` に落ちるが、ADB 26ai の既定プロファイルはパスワード再利用を拒否する(`ORA-28007: The password cannot be reused`)。Terraform 生成パスワードは state で固定なので、**イメージ更新・CI再作成・クラッシュ復帰のたびに必ず発生**する。`bootstrap()` の外側 `except Exception` がこれを飲み込み「ADB 未準備のため20s後に再試行」を25分繰り返して諦めるため、①`migrate()` が実行されない ②`ENABLE_RESOURCE_PRINCIPAL` 検証が走らず `/api/health` の dbchat が `unavailable` のまま、という二次症状になる。しかも**例外を一切ログに出していなかった**ため、プロビジョニング待ちと恒久失敗が見分けられなかった。修正: ORA-28007 は「既にそのパスワード」として続行し、ただし ORA-28007 は「履歴にある」であって「現在値」とは限らないので実ログインで裏取りする。リトライログには必ず例外を載せる。
- **2026-07-28 TTS の Phoenix 決め打ちは誤った機能不全を生む(FIX-58)**: `tts_region` の既定が `us-phoenix-1` 固定だったため、Phoenix 未購読のテナンシでは TTS が常に 503。実測では **us-chicago-1 で合成成功**、ap-osaka-1 / ca-toronto-1 は 404 で、提供リージョンは Phoenix 限定ではなくなっている。既定を空(=デプロイリージョン → us-phoenix-1 の順に試行)にし、明示指定時のみ単一リージョンに固定する。大阪デプロイは従来どおり Phoenix にフォールバックして動く。
- **2026-07-28 「機能ごとの health」は処理プロセスの分裂を織り込まないと偽陽性になる(FIX-58)**: `/api/tts` は API Gateway 経由で **OCI Functions** が処理し、`/api/health` は **Container Instance** が返す。したがって実合成の成否をプロセス内変数に持っても health には届かず、「設定があるから ok」と言い続ける。TTS は `list_voices`(合成しない=課金なし)が提供リージョンでのみ 200 を返し 404/成功が synthesize と一致するため、これを到達性プローブに使える(結果は数分キャッシュ)。同型の注意: `presets` / `dbchat` / `tts` セグメントは Functions 側実装なので、CI 側の状態に依存する診断は成立しない。**さらに `list_voices` は `compartment_id` を省くとテナンシルート扱いになり、リソースプリンシパルでは 404 になる**（ユーザープリンシパルでは通るので見落としやすい。ここでも「ローカルで通る＝CIで通る」ではない）。
- **2026-07-29 OCI Enterprise AI Agent の Hosted Application は Terraform で作れて、ゼロスケールする(PORT-03 調査)**: `docs/setup/hosted-agent-oauth.md` が AGT-04 当時の手動デプロイ前提で書かれているため「Terraform では作れない」と誤解しやすいが、ORM が入れる provider **v8.24.0** に `oci_generative_ai_hosted_application` / `oci_generative_ai_hosted_deployment` があり、`inbound_auth_config.idcs_config{audience, domain_url, scope}`（＝AGT-04 で手作業していた inbound の aud/scope 突合）まで宣言できる。OAuth 側も `oci_identity_domains_app` で `client_type=confidential` + `is_oauth_resource` + `scopes{value,fqs}` を作れ、**`client_secret` は computed 属性**なので Terraform から env へ注入できる。**課金は常設ではない**: [Application comparison](https://docs.oracle.com/en-us/iaas/Content/generative-ai/application-comparison.htm) が Scale to zero: Yes と明記しており、実機(us-chicago-1)でも `scaling_type=CONCURRENCY, min_replica=0, max_replica=2` で作成でき **ACTIVE 後も min_replica=0 が保持される**ことを確認した。API のハマりどころ2つ: ①`inbound_auth_config` は必須(省略で 400 `inboundAuthConfig must not be null`) ②`inbound_auth_config_type` の値は `"IDCS"` ではなく **`"IDCS_AUTH_CONFIG"`**(`"IDCS"` は 400 `Invalid InboundAuthConfigType`)。`list_hosted_applications` は us-chicago-1 / ap-osaka-1 とも 200。
- **2026-07-28 OCI Vector Store はチャンク属性フィルタに対応している(SPIKE-M1)**: 「外部ベクタストアはメタデータを持てない」は誤り。`vector_stores.files.create(..., attributes={...})` で任意メタを付与でき、`vector_stores.search(filters=...)` と Responses の `tools[].filters` で絞り込める(`eq`/`and`/`or`/`gte` は動作、**`in` は 422**: `values` 配列を解釈せず `value` を要求)。返却の `file_search_call.results[]` には attributes・text・score・chunk_id が入る(annotations 側は file_id/filename のみ)。**制限**: 属性は**ファイル単位**(1ファイルが5チャンクに割れても attributes は1種類)・キー最大16・値512文字・文字列/数値/真偽のみ(入れ子不可)・存在しないキーのフィルタは**エラーにならず0件**。チャンク単位のセル範囲が要るなら 1チャンク=1ファイルで取り込むしかない。よって現行 `chat.py:_extract_citations()` が file_id/filename/score しか返さないのは OCI の制約ではなくアプリ側の実装。
- **2026-07-28 DBMS_CLOUD_AI.CREATE_VECTOR_INDEX の attributes に任意キーは載らない(SPIKE-M1)**: 任意メタのつもりで `current_version` を渡すと `ORA-20048: Invalid vector index attribute - current_version`。attributes は**索引の設定項目**であってチャンクのメタではない。`$VECTAB` の ATTRIBUTES(JSON) に入るのは固定6キー(object_name/object_size/last_modified/location_uri/start_offset/end_offset)のみ。回避は①オブジェクト名に埋め込む(文字列パース前提) ②`ALTER TABLE "<IDX>$VECTAB" ADD (...)` で列を足す。ただし②は**索引リフレッシュで入った新規行が NULL になる**(実測: 10行→11行で NULL 1件)ため補完ジョブを回し続ける必要がある。なお走行中パイプラインは `DBMS_CLOUD_PIPELINE.RUN_PIPELINE_ONCE` を拒否する(`ORA-20044: Pipeline must be in stopped state to run in foreground.`)ので STOP→RUN_ONCE→START の順。
- **2026-07-28 DBMS_VECTOR_CHAIN の資格証明ストアは DBMS_CLOUD と別(SPIKE-M1)**: `UTL_TO_EMBEDDING` の `credential_name` に `DBMS_CLOUD.CREATE_CREDENTIAL` で作った名前を渡すと `ORA-20003: error retrieving credential`(ORA-20002 でラップされる)。`DBMS_VECTOR_CHAIN.CREATE_CREDENTIAL(credential_name=>..., params=>json(...))` で別途作る。作れば ap-osaka-1 の ocigenai provider (`/20231130/actions/embedText` + cohere.embed-multilingual-v3.0)で DB 内埋め込みが 1024次元で動く。**ただし「本文が外に出ない」わけではない**: UTL_TO_EMBEDDING は本文を OCI Generative AI の embedText へ送る。得られるのは「アプリ層に本文を持ち出さずに済む」であって、テナント外への送信は起きる。
- **2026-07-28 ops/setup-select-ai.py の ~/.oci/config パーサは複数プロファイルで壊れる(SPIKE-M1で発覚 → 2026-07-29 RP-01 で解消。パーサごと廃止)**: 現在この経路は無い。DB内の資格情報は `OCI$RESOURCE_PRINCIPAL` に統一し、`~/.oci/config` を読んで `DBMS_CLOUD.CREATE_CREDENTIAL` を呼ぶコードを削除した(ADR-0021 / `docs/verification/RP-01.md`)。**症状の見分け方は今も有効なので残す**: `dict(line.split("=",1) for line in config if "=" in line)` は全プロファイルを1つの dict に潰すため、`[DEFAULT]` と `[OTHER]` がある環境では**最後のプロファイル**の user/tenancy/fingerprint/key_file を拾う。結果として作られた credential は署名は通るが別テナンシのものになり、DBMS_CLOUD の**全呼び出しが `ORA-20404: Object not found`**(=OCI の NotAuthorizedOrNotFound を DBMS_CLOUD がこう見せる)になる。Object Storage も GenAI も一様に 404 なら真っ先にこれを疑う。切り分けは `DBMS_CLOUD.SEND_REQUEST` で既知エンドポイント(Object Storage の `/n/<ns>/b/`)を叩き、そこも404なら資格証明側。**ただし ListBuckets は `?compartmentId=<OCID>` が必須**で、付け忘れると認証と無関係に HTTP 400 になり、指定コンパートメントに権限が無ければ ORA-20404 になる(RP-01 実測)。切り分けに使うなら「権限があるはずのコンパートメント」を明示すること。
- **2026-07-28 10行規模ではベクタ索引は使われない(SPIKE-M1・数字の読み方)**: HNSW索引(`ORGANIZATION INMEMORY NEIGHBOR GRAPH DISTANCE COSINE`)を作っても、10行の表に `WHERE current_version='Y'` 付きベクタ検索を投げると実行計画は `TABLE ACCESS STORAGE FULL` + `filter("CURRENT_VERSION"='Y')` で、厳密検索が選ばれる。よって小規模スパイクのレイテンシ実測(16.7ms)は「索引つきベクタ検索の速さ」ではなく「全件走査の速さ」であり、大規模での傾きを示さない。比較資料に載せるときはこの但し書きを必ず付ける。さらに **SQL 単体(16.7ms)と外部ベクタストアの検索API(121.5ms)を直接並べない**: 前者はクエリ埋め込みを含まず、埋め込みAPI単体が別途 84.5ms かかる。同じ土俵にすると ADB 約101〜192.7ms / 外部ストア121.5ms でほぼ同じになり、しかも実行ごとに揺れるので、レイテンシは方式選択の決め手にならない。**そして 3 方式が同じデータ(件数でなく chunk_id の集合)を見ているかを計測前に実測で確かめること**(片方に検証用ファイルが残ったまま数字を並べて比較にならない、を一度やった)。
- **2026-07-28 「消してよいか」の判定を経路ごとに書くと片方が抜ける(SPIKE-M1 の自損事故)**: 片付けスクリプトで OCI 側(バケット/Vector Store/GenAIプロジェクト)は「自分が作ったと台帳に記録した ID だけ消す」ゲートを入れたが、**DB 側(DROP USER/DROP TABLE)は名前一致のまま**だった。その状態で「台帳が空なら消さないこと」を確認する否定テストを `--yes` 付きで走らせた結果、テスト自身が本物の検証用スキーマを削除した(共有ADBの他スキーマには波及せず)。教訓: 破壊操作のゲートは**経路ごとに書かず単一の述語に集約**し、否定テストは「ゲートが無い経路が残っていないか」まで数える(本件は skip 件数を 3→5 に増やして検出できるようにした)。
- **2026-07-29 Hosted Application の `environment_variables.value` は「JSON文字列だがアンマーシャルされない」ので、スカラーを渡すと引用符ごとコンテナに届く(PORT-03 / DEPLOYTEST 実機)**: OCI provider の当該フィールドは `ValidateFunc: validation.StringIsJSON` で **JSON として妥当な文字列しか受け付けない**が、[provider ソース](https://github.com/oracle/terraform-provider-oci/blob/master/internal/service/generative_ai/generative_ai_hosted_application_resource.go)は受け取った文字列を `json.Unmarshal` せず `interface{}` へキャストしてそのまま送る。API 側も文字列を verbatim に保存する。us-chicago-1 で3種類の値を入れて GET し直した実測は次のとおり: `us-chicago-1` → `us-chicago-1`（CLI 直叩きなら素通し）/ `"us-chicago-1"`(引用符付き) → `"us-chicago-1"`(引用符が残る) / `{"OCI_REGION":"us-chicago-1"}` → 同一。つまり Terraform から `jsonencode("us-chicago-1")` を渡すと **コンテナは `"us-chicago-1"` という引用符入りの値を受け取り**、リージョン解決や OCID 参照が静かに壊れる（起動はするので気づきにくい）。回避は **設定を JSON オブジェクト1本にまとめて渡し、コンテナ側で `os.environ` へ展開する**（`JETUSE_AGENT_CONFIG` / `packages/agent-containers/agent_env.py`）。オブジェクトなら往復が一致する。副次効果として「sensitive な map は `for_each` に使えず plan が停止する」という別の制約も同時に回避できる（環境変数ブロックを動的展開しなくて済むため）。なお `inbound_auth_config` の domain URL が実在しないと Application は CREATING のあと **FAILED** になる（作成 API 自体は 201 を返す）。
- **2026-07-29 ホスト型エージェントの destroy は Application 側から消さないと失敗する(PORT-03)**: ACTIVE な Hosted Deployment は直接削除できず、削除できるのは Application で、そのとき Deployment がカスケード削除される（`ops/recreate-agents.sh` に実機記録）。ところが Terraform の destroy 順は「依存する側から」なので、素直に書くと Deployment → Application の順で消そうとして**最初の1手で落ちる**。Deployment を参照する `terraform_data`（＝Deployment より先に destroy される）に destroy-time provisioner を置き、そこで Application を削除して DELETED まで待つ形にする。CLI は `oci raw-request` を使う（Resource Manager 同梱の OCI CLI に `generative-ai hosted-application` サブコマンドが無い版があり得るため、専用サブコマンドに依存しない）。
- **2026-07-29 `oci_generative_ai_hosted_application` は provider 8.24.0 で必ず失敗する（PORT-03 / DEPLOYTEST 実機）**: Hosted Application の create/delete は API 側もサービス側も成功し、work request も `status=SUCCEEDED` / errors 空・リソースは ACTIVE になるのに、Terraform は毎回 `Error: Work Request error ... work request did not succeed, workId: ..., entity: hostedapplication, action: CREATED` で落ちる。原因は provider の照合ロジックで、[`hostedApplicationWaitForWorkRequest`](https://github.com/oracle/terraform-provider-oci/blob/master/internal/service/generative_ai/generative_ai_hosted_application_resource.go) が `strings.Contains(strings.ToLower(res.EntityType), "hostedapplication")` で work request の resources を探すのに対し、サービスが返す `entityType` は **`HOSTED_APPLICATION`**（アンダースコア入り）。小文字化しても `hosted_application` にしかならず一致せず、`identifier` が nil のまま失敗扱いになる。**症状が厄介なのは収束しないこと**: create 失敗でリソースは state に記録されるが tainted になり、次の apply で「削除→再作成」を試みて delete 側でも同じ誤判定で落ちる。回避は **作成・削除を `oci raw-request` で行い、OCID の参照だけ data source（`oci_generative_ai_hosted_applications`）から取る**こと（データソースは list/get のみで work request を待たないため影響を受けない）。8.24.0 が最新で、修正版は無い（2026-07-29 時点）。
- **2026-07-29 `oci raw-request` は `--query` / `--output table` を受け付けない（PORT-03）**: グローバルオプションとして受理はされるが出力には適用されず、レスポンス全体がそのまま出る。local-exec から使うときは `grep -oE '"id": "ocid1\.[^"]*"'` のように素のテキスト処理で抜くしかない（python 等の有無に依存しないので、むしろ Resource Manager の実行環境では好都合）。
- **2026-07-29 公開スタックの画像タグを機能ごとに別管理すると壊れる（PORT-03）**: エージェント画像だけを差し替えて apply したところ、API コンテナは既存の `jetuse-api:latest` のままだったため `/api/health` に `capabilities.agents` が出ず、アプリ側の新機能が丸ごと効いていなかった。API とエージェントは invoke ステート（`project_ocid` 等）の契約を共有するので、**API / Functions ルーター / 3SDK エージェントは単一の `image_tag` で束ねて同時に上げる**。配布ZIP生成時に `scripts/package-orm-stacks.sh` が commit SHA へ固定する。
- **2026-07-29 E2E ハーネスのログイン待ちは固定秒数にしてはいけない（PORT-03）**: `public-deploy.mjs` はサインイン後 8 秒固定で待っていたが、IDCS → authorize → アプリのリダイレクトが終わらないうちに API を叩き、`/api/health` 以下が軒並み 401 になった。アプリのホストに戻るまで `waitForURL` で待ち、さらに SPA が認可コードをトークンに交換し終える（sessionStorage/localStorage に oidc キーが現れる）まで待つ必要がある。交換前に叩くと `invalid token: DecodeError` になる。あわせて `make-assets.mjs` は OCR 画像しか作っておらず、音声シナリオが要求する `tone.wav` が無くてハーネスが異常終了していた（README にも記載が無かった）ので、同スクリプトで生成するようにした。
- **2026-07-30 メタデータ絞り込み付きベクタ検索の実行計画は索引構成で変わる（RAGM-02）**: 50,000 チャンクの `VECTOR` 表で `ORDER BY VECTOR_DISTANCE ... FETCH APPROX FIRST` に `WHERE` を付けたとき、**素の表では計画が実行ごとに揺れた**（同じ手順の 2 回の計測で `TABLE ACCESS STORAGE FULL`（索引不使用）と `VECTOR INDEX HNSW SCAN IN-FILTER` の両方が出た。ADB のオンライン統計収集のタイミングによると思われる）。**フィルタ列に B木索引を張り、`owner_sub` 込みで絞ると 2 回とも `VECTOR INDEX HNSW SCAN PRE-FILTER`**（メタデータ索引で前絞り → 近似検索）で安定した。recall@10 は索引が使われるどの構成でも厳密検索と一致（1.00）。高選択度（0.3% 程度）の条件では B木の `INDEX RANGE SCAN` + 厳密検索が選ばれ、それで十分速い（18 ms）。**「ベクタ索引を作れば効く」ではなく「絞り込みに使う列にも索引が要る」**。なお、ベクタ索引が無いときは同じ `FETCH APPROX FIRST` の SQL が厳密検索へ落ちて結果は一致する（実測）ので、索引作成の失敗で取り込みを止める必要はない。検証は `docs/verification/RAGM-02.md`。
- **2026-07-30 `:file` をバインド名にすると ORA-01745（RAGM-02）**: フィルタのキー名をそのままバインド名に使う実装（`WHERE doc_file = :file`）は `ORA-01745: invalid host/bind variable name` で落ちる。予約語と衝突するため。バインド名は接頭辞を付けて生成する（本実装は `:flt_file`）。単体テストで固定してある。
- **2026-07-30 同一セッションで DDL を打った直後の最初の `EXPLAIN PLAN` は ORA-00900（RAGM-02）**: `CREATE INDEX` / `DROP INDEX` の直後に `EXPLAIN PLAN ... FOR <SELECT>` を実行すると `ORA-00900: invalid SQL statement` になり、もう一度同じ文を投げると通る（再現性あり・カーソルを作り直しても同じ）。検証スクリプトでこれを握り潰すと「計画取得不可」で証跡が空になり、測っていないのに測ったように見える。**1 回だけ引き直す**のが実務上の対処。
- **2026-07-30 DB 内埋め込み `UTL_TO_EMBEDDING` は `OCI$RESOURCE_PRINCIPAL` では動かない（RAGM-02）**: スキーマに ACL（`http` / `connect` / `resolve`）とリソースプリンシパルを付けても `ORA-24247: Network access denied by access control list (ACL)` になる。`DBMS_VECTOR_CHAIN` の資格証明ストアは `DBMS_CLOUD` と別で（SPIKE-M1 の ORA-20003）、`DBMS_VECTOR_CHAIN.CREATE_CREDENTIAL` で **API キー**の資格証明を作らないと通らない。ADR-0021 で API キーを DB に焼く経路は廃止したので、RAG の `adb` バックエンドは**クライアント側埋め込み**に決めた。
- **2026-08-01 OCI Document Understanding の日本語 OCR は英数字の連なりに空白を挟む（PREP-03 実測）**: `BRG-7781` → `BRG - 778 1`、`LOT-2026-0518` → `LOT - 2026 - 0518`、`P-204` → `P - 20 4`。文字認識そのものは正しく（mean confidence 0.99）、日本語の語によるベクタ検索・生成回答には影響しない（実測で検索は当たり回答も正しい）。効くのは**型番・ロット番号の完全一致検索**で、`WHERE body LIKE '%BRG-7781%'` は当たらない。OCR 出力を書き換えて正規化すると「原文に何が書いてあったか」が失われるので、比較側で空白を無視するのが素直。スキャン文書の検索デモで型番を検索キーに選ぶと、この癖のせいで「OCR が効いていない」ように見える。**生成回答の識別子も揺れる**: 同じ入力で 2 回測って `BRG-778`（末尾欠落）と `BRG-7781`（一致）に分かれた（チャンク本文も空白入りのまま保存されるので、引用に出るのは `BRG - 778 1`。空白を除けば原文と一致する＝文字は落ちていない）。
- **2026-08-01 テキスト層の有無は「呼び出し回数」でしか証明できない（PREP-03）**: 「テキスト層のある PDF は OCR を通していない（＝課金していない）」は、抽出結果を見ても分からない（どちらの経路でも本文は返る）。E2E では `docunderstand.ocr` / `ocr_vlm` を数えるラッパで包み、操作の前後で 0 回であることを証跡にした。コスト面の主張は、結果ではなく呼び出しを測る。
- **2026-08-01 「秘密は Vault だけ」は URL 欄で破れる(TOOL-01)**: 外部 HTTP ツールの認証は Vault 参照に寄せたが、**PAR や署名付き URL はパス/クエリ自体が資格情報**なので、URL 欄に入れた瞬間に平文で DB へ保存され API 応答にも出る。秘密の入口はヘッダだけではない。同種の設計（外部の宛先をユーザーに登録させる機能）では、**URL も秘密になりうる**前提で保存・表示・ログ出力を設計すること。JetUse では実害（登録者本人しか見られない）と代償（隠すと切り分け不能）を比べて**受容**し、specs/17 に「そういう URL を登録しない」と明記した。
- **2026-08-03 リージョン フォールバックは「未提供(4xx)」だけでなく「障害(5xx)」も次候補へ回さないと片肺で死ぬ(FIX-TTS)**: TTS の `synthesize()` は 401/403/404 のときだけ次リージョンを試し、それ以外は即打ち切っていた。us-chicago-1 の TTS が **500 `Connection refused: <OCI内部エンドポイント>`**（サービス側の内部障害）を返す一方 us-phoenix-1 では同じ呼び出しが成功する状況で、`/api/tts` が常に 503 になった。**未提供と障害はどちらも「この候補では無理」でしかなく、他候補を試す価値は同じ**。逆に 400 系（リクエスト誤り）はリージョンを変えても直らないので即打ち切ってよい。切り分けを速くするため、全滅時のメッセージには**直近の HTTP status/code を必ず載せる**（「未購読の可能性」だけだと購読設定を疑わせる）。
- **2026-08-03 プロセスが分かれていると「解決済みリージョン」のキャッシュも分かれる(FIX-TTS)**: `/api/tts` は Functions、`/api/health` は Container Instance が処理する（`fn_router_segments = ["presets","dbchat","tts"]`）。CI 側はプローブで Phoenix に解決済みなので health は `verified:true` を返すのに、Functions 側は `_resolved_region` を持たずデプロイリージョンから試すため落ちる、という**health は緑なのに機能だけ死ぬ**状態が起きた。プロセス内キャッシュで挙動が変わる設計は、**どのプロセスがそのエンドポイントを処理するか**を必ず対にして考える（FIX-58 の 2026-07-28 の記述の続き）。
- **2026-08-20 バインド名の予約語は再発する。fake の cursor では絶対に出ない(VID-05)**: 修正履歴の `INSERT ... VALUES (:id, :sid, :f, :b, :a, :by)` が実 ADB で `ORA-01745`。`BY` が予約語（2026-07-30 の `:file` と同じ穴）。**単体テストの fake cursor は SQL を解釈しない**ので、PATCH と confirm が実環境の 1 回目で揃って 503 になるまで気づけなかった。列名をそのままバインド名にする書き方（`SET place = :place` など）は、列名が予約語に当たった瞬間に同じ穴に落ちる。**新しい SQL を足したら、モックではなく実 DB に 1 回通す**。
- **2026-08-20 `array.array("f")` は float32 の範囲外を例外ではなく `inf` にする(VID-05)**: `array.array("f").append(1e40)` は `OverflowError` ではなく `inf` を格納する。埋め込みの値検証を「例外が出なければ良い」で書くと、無限大がそのまま `VECTOR(1024, FLOAT32)` 列に入り、距離が計算できない行が黙って生まれる（`VECTOR_DISTANCE` の結果が使えず、その場面は検索から消えるのに理由が残らない）。変換**した後**の値を `math.isfinite` で見ること。
- **2026-08-21 OCI API Gateway の要求本文の上限は 20 MiB（VID-07 実測）**: デモ・スタックのゲートウェイに multipart を投げ、本文長を 1 バイト単位で組み立てて測ったところ、**20,971,520 バイトは通り 20,971,521 バイトが `{"code":413,"message":"Payload Too Large"}`**。境界は **20 MiB = 20,971,520 バイト**ちょうどで、これは**本文全体**（ファイル本体＋他のフォーム項目＋multipart の縁取り）に効く。アプリ側の上限をこれより大きく書くと、**利用者にはアプリの案内が一切届かない**（ゲートウェイが先に切るため、本文形式も `detail` ではなく `code`/`message` になる）。切り分け方: **アプリが必ず 4xx を返す入力**（例: 拡張子違い）で投げると、`422` が返れば通過・`413` ならゲートウェイ、と台帳を汚さずに測れる。回避は**本文をゲートウェイに通さない**こと（書き込み専用 PAR を発行してブラウザから Object Storage へ直接 PUT）。
- **2026-08-21 PAR の発行権限は `manage objects` に含まれない。欠けると「バケットが無い」と言われる（VID-07）**: リソースプリンシパルに `manage objects` + `read buckets` を与えていても `CreatePreauthenticatedRequest` は **404 `BucketNotFound`（"…or you are not authorized to access it"）** で落ちる。PAR_MANAGE は object ではなく **bucket 側の permission** だから。同じ経路の `put_object` は 200 で通るので、**バケット不在に見えて実は権限不足**という紛らわしい形になる。最小の付与は `Allow dynamic-group <dg> to manage buckets in compartment id <c> where request.permission='PAR_MANAGE'`（`manage buckets` を丸ごと与えるとバケット削除まで渡る）。**この穴は SDK 直叩きの E2E では絶対に出ない**（手元のユーザープリンシパルには権限がある）。JetUse では映像の再生 URL（VID-01）が配備環境でだけ 500 になっており、ゲートウェイ経由で叩いて初めて判った。
- **2026-08-21 ブラウザ → Object Storage の直接 PUT は CORS 設定なしで通る（VID-07 実測）**: PAR への `OPTIONS`（preflight）は `access-control-allow-origin: *` / `allow-methods: POST,PUT,GET,HEAD,DELETE,OPTIONS` / `allow-headers: content-type` を返し、実 PUT の応答にも同じヘッダが付く。バケット側に CORS 設定は要らない（そもそも OCI Object Storage に設定項目が無い）。`ObjectWrite` の PAR は **GET / HEAD とも 404** になるので、書き込み専用として配れる。PUT した `Content-Type` はオブジェクトのメタデータに残り、`head_object` で検証できる（＝サーバ側で「申告どおりのものが入ったか」を確かめられる）。
