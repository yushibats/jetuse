# secrets / environment
.env
.env.local
*.pem
*.key
# tfvars は秘密値を含むため全て無視(雛形 *.tfvars.example のみ追跡)
*.tfvars
!*.tfvars.example

# python
.venv/
.coverage
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
*.egg-info/
build/

# node
node_modules/
dist/
# **packages/web/dist は追跡しない**（2026-08-22 / OSS 化に向けて）。
# 以前は ORM ワンクリック配布の都合で追跡し、陳腐化を ci.yml が検出していた。しかし
# **ビルド出力は環境で一致しない** —— macOS と CI(Linux) でチャンクのハッシュが変わり、
# ソースを1文字も違えていなくても「dist がソースと一致しません」で落ちる。実際この方式で
# CI が5回落ち、そのたびに Linux コンテナでビルドし直して合わせていた。
# 外部の貢献者は環境を揃えられないため、**OSS では貢献の入口を塞ぐ**。
# 配布物は release.yml が main でビルドして zip へ同梱する（もともとそうしている）。
.vite/

# terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfplan
.terraform.lock.hcl

# misc
*.log
.DS_Store
tmp/

# Obsidian 出力先の絶対パス（環境依存・共有しない・ハンドオフ ch.5）
.obsidian-dir

# spike audio artifacts (archived)
docs/archive/spikes/data/*.mp3
docs/archive/spikes/data/*.wav
.playwright-mcp/

# 個人フィードバック原文はリポジトリに含めない(ローカル管理)
/docs/feedbacks/

# loop 成果物（stage-runner 統合で混入防止）
.current_run_id

# 人間向け報告書 HTML（ADR-0018）。報告書は仕様でも証跡でもなく「読み物」なので、
# 原本は runs/ に生成しつつ git には入れず、報告パイプで各自の閲覧環境へ配置する。
runs/**/*.html

# codex レビューの中間生成物とターン差分（ADR-0029）。
# 判定と結論は review-<n>.json / STATE.md / summary.md に残るので、証跡の追跡性は落ちない。
# ここは公開リポジトリで、実 OCID の主要な混入経路でもある（マスク漏れは
# ops/check-no-real-ocid.sh が別途止める）。既追跡分は同 ADR で追跡解除した。
runs/**/reviews/*.raw.txt
runs/**/reviews/*.payload.txt
runs/**/reviews/*.input.diff
runs/**/diffs/turn-*.diff
docs/verification/*.html

# 無人 claude 実行ログ（ヘッドレス委譲・ハンドオフ ch.4）
.agent-runs/

# SPIKE-M1 の検証リソース台帳（OCID を含む運用状態。証跡ではない）
.spike-m1-registry.json

# ループの「現在状態」。タスクごとの worktree が同じパスを全面上書きするため、追跡すると
# 並列ループを main へ入れるたびに必ず衝突する（実例: SPIKE-M1 / RP-01 / RAGM-02 で 3 回）。
# 上位ループ(stage-runner)は元からコミット対象外にしており、その規律に揃える。
# 不変の履歴は runs/<run-id>/ にあり、完了時に runs/<run-id>/STATE.md へ写しを残す。
STATE.md
